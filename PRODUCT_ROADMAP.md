# PXM 제품 로드맵 및 시나리오 (Product Roadmap & Scenarios)

이 문서는 PXM(Process eXperience Manager) 워크플로우 엔진이 상용 제품 수준으로 발전하기 위해 필요한 기술적 과제, 기능적 요구사항, 그리고 실제 비즈니스 활용 시나리오를 정의합니다.

---

## 🚀 1. 기술적 완성도 (Core Engineering)

> "안정적으로 돌아가는가?"

### 데이터 흐름 및 검증

- [ ] **변수 바인딩 (Variable Interpolation)**: 이전 노드의 출력값(폼 데이터, API 응답)을 다음 노드의 입력값이나 조건에 동적으로 매핑 (`{{data.amount}}`).
- [ ] **재시도 정책 (Retry Logic)**: Service Task 실패 시 설정된 정책(Exponential Backoff)에 따라 자동 재시도.
- [ ] **트랜잭션 관리**: 워크플로우 상태 변경의 원자성 보장.

### 안정성 및 복구

- [ ] **Crash Recovery**: 서버 비정상 종료 후 재시작 시, 진행 중이던(Waiting/Running) 인스턴스 복구 및 재개.
- [ ] **Dead Letter Queue (DLQ)**: 영구적으로 실패한 작업을 관리자가 확인하고 수동으로 재처리하거나 폐기하는 기능.

### 유효성 검사 (Validation)

- [ ] **템플릿 무결성 체크**: Start/End 노드 존재 여부, 고립된 노드(Disconnected nodes) 탐지.
- [ ] **순환 참조 방지**: 무한 루프에 빠질 수 있는 흐름 경고.

---

## 🏢 2. 엔터프라이즈 기능 확장 (Enterprise Features)

> "기업 고객이 돈을 내고 쓸 만한가?"

### 조직 및 사용자 관리 (IAM)

- [ ] **조직도(Organization Chart)**: 부서, 직책, 직무 구조화. 결재선을 "사람"이 아닌 "직책(예: 재무팀장)"으로 지정하는 기능.
- [ ] **회원 시스템 & RBAC**: 관리자(템플릿 생성), 운영자(모니터링), 일반 사용자(요청/결재) 권한 분리.
- [ ] **SSO/LDAP 연동**: Google Workspace, MS Azure AD, Okta 등 기업 계정 연동.

### 알림 및 연동 (Notification & Integration)

- [ ] **채널 연동**: 승인 요청, 반려, 에러 발생 시 Slack, MS Teams, Email, Webhook 알림 발송.
- [ ] **서드파티 플러그인**: Jira, Google Sheets, Slack 등 자주 쓰는 서비스용 사전 정의된 Service Node 제공.

### 운영 및 감사 (Ops & Audit)

- [ ] **감사 로그 (Audit Logs)**: 누가 언제 템플릿을 수정했는지, 누가 강제로 승인 처리를 했는지 기록 (보안 필수 요건).
- [ ] **버전 관리 (Versioning)**: 템플릿 수정 이력 관리 및 문제 발생 시 이전 버전으로 롤백(Rollback).
- [ ] **대시보드**: 워크플로우별 평균 처리 시간, 병목 구간, 실패율 통계 시각화.

---

## 💼 3. 실제 고객사 활용 시나리오 (Business Scenarios)

### Scenario A: IT 인프라/접근 권한 자동화 (Tech 기업)

**상황**: 신규 입사자가 들어오거나 개발자가 서버 접근 권한이 필요할 때.

1. **요청**: 개발자가 "AWS 운영 서버 접근 권한 신청 (사유: 배포)" 폼 작성.
2. **승인(Approval)**: 소속 팀장에게 슬랙으로 알림 -> 팀장 승인.
3. **보안 검토(Gateway)**: 만약 신청 기간이 30일 이상이면 보안팀장 전결 필요, 이하면 자동 통과.
4. **자동 처리(Service)**: 승인 완료 시, 엔진이 **AWS IAM API**를 호출하여 권한 부여.
5. **완료**: 신청자에게 "권한 부여 완료" 메일 발송 및 보안 감사 로그 자동 기록.

### Scenario B: 경비 지출 결재 및 처리 (Finance/General)

**상황**: 직원이 법인카드를 사용하고 지출 결의를 올릴 때.

1. **요청**: 영수증 사진 업로드 및 금액 입력.
2. **조건 분기(Gateway)**:
   - 금액 < 10만원: 자동 승인.
   - 10만원 ~ 100만원: 팀장 승인.
   - 100만원 >: 본부장 승인.
3. **연동(Service)**: 승인 완료 시, 회사의 **ERP 시스템(SAP 등)** API를 호출하여 전표 자동 생성.
4. **알림**: "이번 달 남은 예산은 OOO원 입니다" 알림 발송.

### Scenario C: 보안 관제 및 사고 대응 (SOAR - Security Orchestration)

**상황**: 해킹 시도가 감지되었을 때 빠른 차단.

1. **트리거(Start - Webhook)**: 보안 모니터링 툴(Splunk 등)에서 이상 징후 감지 시 웹훅으로 워크플로우 시작.
2. **초동 조치(Service)**: 즉시 방화벽 API를 호출하여 해당 IP 임시 차단.
3. **분석 요청(Approval)**: 보안 담당자에게 "IP 1.2.3.4가 공격 의심됨. 영구 차단 할까요?" 승인 요청.
4. **후속 조치**:
   - 승인 시: 블랙리스트 DB에 등록 및 관할 기관 신고 메일 자동 발송.
   - 반려 시(오탐): 방화벽 임시 차단 해제 API 호출.

---

### 📝 결론

현재 PXM 엔진은 **핵심 실행 엔진(Kernel)**의 역할을 하고 있습니다.
고객에게 판매되는 "완제품"이 되기 위해서는 **조직 연동(사람)**과 **외부 시스템 연동(API)**, 그리고 이 모든 과정을 투명하게 관리하는 **운영 도구**가 덧붙여져야 합니다.
