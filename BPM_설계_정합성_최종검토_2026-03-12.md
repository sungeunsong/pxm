# BPM 설계 정합성 최종 검토 (2026-03-12)

## 1) 검토 목적
`BPM_설계_문서.pdf`의 설계 의도(1~11장)가 현재 코드/DB/API/UI에 얼마나 반영되어 있는지 최종 점검한다.

## 2) 검토 범위
- 설계 원문: `BPM_설계_문서.pdf` (51p)
- 런타임/DB: `apps/engine/src/main.rs`, `infra/db/migrations/*.sql`
- API: `apps/api/src/**/*`
- Web: `apps/web/src/**/*`
- 계약 타입: `packages/contracts/src/events.ts`

## 3) 판정 기준
- `일치`: 설계 핵심 의미가 코드/데이터모델/실행경로에 구현됨
- `부분 일치`: 일부 구현됐으나 핵심 의미론/확장요건이 빠짐
- `불일치`: 설계 핵심 구조와 현재 구현 방식이 다름

---

## 4) 섹션별 최종 판정

## 4.1 1장 Full Instance vs Job
판정: `부분 일치`

근거
- Job Queue + `run_at` 기반 폴링 및 실행: `apps/engine/src/main.rs:1255`, `apps/engine/src/main.rs:1263`
- `FOR UPDATE SKIP LOCKED` 사용: `apps/engine/src/main.rs:1265`
- Job 완료 후 다음 Job(RESUME) 생성: `apps/engine/src/main.rs:671`
- 설계의 핵심인 instance-level lock/lease는 실행 경로 미적용(함수만 존재): `apps/engine/src/main.rs:1334`, `apps/engine/src/main.rs:1356`

요약
- "Job 중심" 방향은 맞지만, "동일 인스턴스 순차 실행 보장"의 구현 완성도가 부족하다.

## 4.2 2장 Node 모델
판정: `부분 일치`

근거
- 타입 기반 노드 집합(Start/Service/Timer/Gateway/Approval/End) 사용: `apps/web/src/flow-designer/form-types.ts:51`
- 엔진도 노드 타입 분기 실행: `apps/engine/src/main.rs:374`
- Approval 노드는 Task 생성 후 WAITING 전환: `apps/engine/src/main.rs:493`, `apps/engine/src/main.rs:519`
- Timer 노드는 예약 Job 생성 후 WAITING 전환: `apps/engine/src/main.rs:976`, `apps/engine/src/main.rs:1007`
- Service 노드 설정(URL/method/headers 등)을 런타임에서 사용하지 않고 하드코딩 endpoint 호출: `apps/engine/src/main.rs:757`
- Start 트리거 다양성(Form/Manual/Webhook/Timer Start) 중 Webhook Start API 없음(현 라우트는 templates/instances 중심): `apps/api/src/templates/templates.controller.ts:53`, `apps/api/src/instances/instances.controller.ts:20`

요약
- 노드 타입 구조는 맞지만, Service/Start 트리거 확장 요구는 미반영.

## 4.3 3장 Edge 모델
판정: `불일치`

근거
- 설계 의도는 Edge가 `condition/isDefault`를 소유해야 함.
- 현재는 Gateway 노드가 condition 소유: `apps/web/src/flow-designer/form-types.ts:75`, `apps/web/src/flow-designer/NodePropertiesForm.tsx:133`
- 엔진도 `node.data.condition`만 평가: `apps/engine/src/main.rs:547`, `apps/engine/src/main.rs:549`
- Edge는 true/false 라벨/핸들 중심: `apps/web/src/flow-designer/FlowCanvas.tsx:201`, `apps/engine/src/main.rs:624`

요약
- Edge 중심 분기모델이 아니라 Gateway 중심 boolean 분기모델이다.

## 4.4 4장 Gateway 모델
판정: `불일치`

근거
- UI에는 gatewayType(XOR/AND/OR) 존재: `apps/web/src/flow-designer/NodePropertiesForm.tsx:126`
- 엔진에서 gatewayType 사용 없음(검색 결과 없음)
- 엔진 분기는 true/false 2분기 + fallback: `apps/engine/src/main.rs:569`, `apps/engine/src/main.rs:572`, `apps/engine/src/main.rs:575`
- AND/OR fork, Join 대기(특히 OR Join의 "실행된 경로만 대기") 로직 없음

요약
- Gateway 의미론은 설계 대비 가장 큰 갭 중 하나.

## 4.5 5장 Process Instance 모델
판정: `부분 일치`

근거
- 인스턴스 생성 + START job + outbox 생성 트랜잭션 처리: `apps/api/src/templates/templates.controller.ts:90`, `apps/api/src/templates/templates.controller.ts:100`, `apps/api/src/templates/templates.controller.ts:117`
- 상태 전이(CREATED/RUNNING/WAITING/COMPLETED/FAILED) 구현: `apps/engine/src/main.rs:334`, `apps/engine/src/main.rs:519`, `apps/engine/src/main.rs:688`, `apps/engine/src/main.rs:882`
- DB 상태 enum 코멘트에는 `TERMINATED` 없음: `infra/db/migrations/001_init.sql:6`
- Runtime 구성요소 중 token/execution_log 실체 테이블 부재

요약
- 기본 인스턴스 라이프사이클은 동작하지만 설계의 Runtime 루트 모델(토큰/로그 포함)은 미완성.

## 4.6 6장 Token 기반 실행 모델
판정: `불일치`

근거
- 현재 실행 위치는 `ctx.cursor` 단일 포인터: `apps/engine/src/main.rs:314`
- 토큰 테이블 없음: `infra/db/migrations/001_init.sql:1`
- 코드에 "추후 token 테이블 이동" 확장 포인트 명시: `apps/engine/src/main.rs:734`
- 이벤트의 token_id도 실토큰이 아니라 고정값 사용 사례 존재: `apps/engine/src/main.rs:997`

요약
- 설계의 핵심인 병렬 토큰 실행/합류 모델이 현재 구조로는 불가.

## 4.7 7장 Execution Context 모델
판정: `부분 일치`

근거
- Instance-level 공유 context는 존재(`ctx jsonb`): `infra/db/migrations/001_init.sql:7`
- Start 폼 입력이 실행 context로 전달됨: `apps/api/src/templates/templates.controller.ts:82`
- Service 결과 저장은 있으나 구조 표준(`input/vars/tasks/result`) 강제 없음: `apps/engine/src/main.rs:823`

요약
- 공유 context는 맞지만 데이터 구조 규약/병렬 충돌 관리 패턴은 미구현.

## 4.8 8장 Execution Log / Event 모델
판정: `부분 일치`

근거
- Event Outbox + SSE 스트림 구현: `infra/db/migrations/001_init.sql:31`, `apps/api/src/instances/instances.controller.ts:39`
- Execution Log 별도 계층(테이블/저장) 없음
- 설계상 분리돼야 하는 internal log vs external event가 현재는 outbox 중심 단일 계층

요약
- 이벤트 전달은 가능하지만 운영/디버깅용 내부 실행 로그 계층이 빠져 있다.

## 4.9 9장 Failure / Retry 모델
판정: `부분 일치`

근거
- RetryPolicy(최대시도, backoff, jitter) 구현: `apps/engine/src/main.rs:24`, `apps/engine/src/main.rs:149`
- 실패 시 retry job 재스케줄(`run_at`) 구현: `apps/engine/src/main.rs:914`
- RETRY_SCHEDULED 이벤트 발행: `apps/engine/src/main.rs:929`
- 재시도 고갈 시 NODE_FAILED/INSTANCE_FAILED 처리: `apps/engine/src/main.rs:864`, `apps/engine/src/main.rs:874`
- 설계 예시의 `RETRY_STARTED`, `RETRY_EXHAUSTED` 이벤트는 미구현

요약
- 재시도 엔진은 실용 수준이나 이벤트 체계는 설계와 일부 다름.

## 4.10 10장 Concurrency / Lock 모델
판정: `불일치`

근거
- Job 중복 실행 방지(SKIP LOCKED)는 적용: `apps/engine/src/main.rs:1265`
- Advisory lock/lease/heartbeat 함수는 존재: `apps/engine/src/main.rs:1334`, `apps/engine/src/main.rs:1356`, `apps/engine/src/main.rs:1431`
- 하지만 실제 job 실행 루프에서 lock/lease 획득/해제 호출 없음: `apps/engine/src/main.rs:255`
- stale RUNNING reclaim는 동작 중: `apps/engine/src/main.rs:1454`

요약
- 설계의 핵심 안전장치가 "코드에 있으나 미배선" 상태다.

## 4.11 11장 Data 모델
판정: `불일치`

근거
- 현재 핵심 엔티티: `process_instance`, `engine_jobs`, `event_outbox`, `tasks`, `workflow_template`: `infra/db/migrations/001_init.sql:3`, `infra/db/migrations/002_workflow_template.sql:4`
- 설계 엔티티 `process_definition`, `token`, `execution_log` 부재
- `engine_jobs`에도 설계의 `token_id` 컬럼 없음: `infra/db/migrations/001_init.sql:17`
- `engine_jobs.type`는 주석상 START/RESUME/TIMER/RETRY 중심: `infra/db/migrations/001_init.sql:20`

요약
- 이름/관계/정규화 수준이 설계 데이터모델과 다르다.

---

## 5) 설계 외 추가 위험(2차 점검에서 재확인)
1. Task 완료 이벤트 불일치
- 계약/클라이언트는 `TASK_COMPLETED`를 기대: `packages/contracts/src/events.ts:11`, `apps/web/src/outbox/useInstanceStream.ts:37`
- 서버 emit 없음(검색 기준), Task 생성만 있음: `apps/engine/src/main.rs:510`

2. Approval 완료 경로 리스크가 주석으로 남아 있음
- API 코드에 무한루프 위험 인지 주석 존재: `apps/api/src/tasks/tasks.controller.ts:58`

3. 사용되지 않는 SSE 컨트롤러 파일 존재
- `SseController` 정의는 있으나 AppModule 미등록: `apps/api/src/sse/sse.controller.ts:5`, `apps/api/src/app.module.ts:8`

4. Web 빌드 실패 상태(운영 안정성 이슈)
- JSX 내부 `console.log`로 빌드 오류 유발 지점: `apps/web/src/flow-designer/ExecutionModal.tsx:231`

---

## 6) 최종 결론
- 전체 판정: `설계와 부분 호환` (핵심 런타임 모델은 미정합)
- 특히 다음 4개는 "중대 갭"이다.
  1. Edge 조건/기본경로 모델 미정합
  2. Gateway XOR/AND/OR/Join 의미론 미구현
  3. Token 런타임 모델 미구현
  4. Instance lock/lease 실행 경로 미적용

즉, 현재는 "데모/기본 흐름"은 가능하지만, 설계서가 목표한 엔진 안정성/확장성/정합성 수준에는 도달하지 못했다.

---

## 7) 권고 (재작성 여부 판단)
- 전면 폐기보다는 `점진 재구성` 권고.
- 유지: 템플릿 CRUD, 인스턴스 생성/시작, outbox+SSE 기본 파이프
- 재구성 우선: 실행모델 코어(Edge/Gateway/Token/Lock) + 데이터모델 정규화

