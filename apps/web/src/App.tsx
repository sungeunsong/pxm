import { ComponentShowcase } from './ComponentShowcase';
// import { RuntimeTrace } from './workflow/RuntimeTrace';

function App() {
  // Temporarily showing ComponentShowcase to test design system
  // Switch back to RuntimeTrace when ready
  return <ComponentShowcase />;
  // return <RuntimeTrace />;
}

export default App;
