# 시연 전 초기화

프로젝트 루트에서 아래 명령을 실행한다.

```bash
pnpm demo:reset
```

초기화가 끝나면 데모 그룹, 계정, 워크플로우와 대리 결재 설정이 기준 상태로 복구된다. 진행 중인 데모 실행이 있으면 초기화가 중단되므로 해당 실행을 완료하거나 종료한 뒤 다시 실행한다. Mailpit에 쌓인 메일은 삭제되지 않으므로 시연할 때 가장 최근 메일을 확인한다.

# 대리 결재와 기한 알림 시연

## 사용할 화면

- PXM: http://localhost:5174
- Mailpit: http://localhost:8025
- 워크플로우: `실습 3 · 대리 결재와 기한 알림`

서버가 내려가 있다면 MongoDB, API, Engine, Web, Mailpit과 데모 Mock 서비스를 먼저 실행한다.

## 계정

| 역할 | 아이디 | 확인할 내용 |
|---|---|---|
| 신청자 | `demo-requester1` | 워크플로우 실행과 처리 결과 확인 |
| 원래 승인자 | `demo-approver1` | 위임된 결재가 자신의 결재함에 나타나지 않는지 확인 |
| 대리 결재자 | `demo-delegate1` | 대리 결재 배지와 원래 승인자 정보를 확인하고 승인 |
| 그룹 관리자 | `demo-secadmin` | 기한 초과 후 상위 알림 확인 |

비밀번호는 로컬 전용 파일에서 확인한다.

```bash
jq -r '.accounts | to_entries[] | "\(.key)\t\(.value)"' .env.demo-access.json
```

로그인 전환을 빠르게 하려면 일반 창, 시크릿 창, 별도 브라우저 프로필을 나누어 사용한다.

## 시연 순서

1. `demo-requester1`로 로그인한다.
2. **요청하기**에서 `실습 3 · 대리 결재와 기한 알림`을 선택한다.
3. 사번 `E-1001`, 권한 `read`, 대상 시스템 `개발 포털`을 입력하고 요청한다.
4. **내 요청**에서 실행이 승인 대기 상태인지 확인한다.
5. `demo-approver1`로 로그인해 새 결재가 원래 승인자의 결재함에 나타나지 않는지 확인한다.
6. `demo-delegate1`로 로그인한다.
7. **내 결재함**에서 요청에 `대리 결재` 배지가 표시되고 원래 승인자가 `demo-approver1`인지 확인한다.
8. 바로 승인하지 않고 1분 이상 기다린다.
9. Mailpit에서 `demo-delegate1@pxm.local`로 발송된 기한 독촉 메일을 확인한다.
10. 다시 1분 정도 기다린 뒤 `demo-secadmin@pxm.local` 대상 상위 알림을 확인한다.
11. 대리 결재자 화면에서 요청을 승인한다.
12. 신청자 화면으로 돌아가 워크플로우가 완료됐는지 확인한다.

## 설명할 핵심

- 대리 결재는 같은 그룹의 활성 사용자에게 기간과 워크플로우 범위를 정해 설정한다.
- 이 시연의 위임은 해당 워크플로우 하나에만 적용된다.
- 결재 요청에는 대리 결재 여부와 원래 승인자가 함께 남는다.
- 처리 기한이 지나면 현재 담당자에게 먼저 알리고, 계속 처리되지 않으면 그룹 관리자에게 알린다.
- 현재 시연 데이터는 처리 기한 1분, 상위 알림 유예 시간 1분으로 설정돼 있다.

## 빠른 시연

시간이 부족하면 8~10단계의 기한 알림 확인을 생략하고, 대리 결재자가 요청을 받은 뒤 승인하는 흐름까지만 보여준다. 기한 알림은 Mailpit에 남아 있는 최근 메일로 별도 설명할 수 있다.
