# PXM 시연 가이드

## 1. 시연 전 초기화

프로젝트 루트에서 가장 먼저 실행한다.

```bash
pnpm demo:reset
```

초기화가 끝나면 데모 그룹, 계정, 자격증명, 승인된 JS 라이브러리, 워크플로우 4개, 입력 프리셋과 대리 결재 설정이 기준 상태로 복구된다. 진행 중인 데모 실행이 있으면 초기화가 중단되므로 해당 실행을 완료하거나 종료한 뒤 다시 실행한다.

초기화 후 다음 화면이 열리는지 확인한다.

| 용도 | 주소 |
|---|---|
| PXM 웹 | http://localhost:5174 |
| Mailpit | http://localhost:8025 |
| 모의 권한 반영 서비스 | http://127.0.0.1:3020 |

MongoDB, API, Engine, Web, Mailpit과 데모 Mock 서비스가 모두 실행 중이어야 한다. 데모 Mock 서비스가 내려가 있으면 별도 터미널에서 다음 명령을 유지한다.

```bash
pnpm demo:service
```

### 공통 계정

| 역할 | 아이디 | 주로 사용하는 시연 |
|---|---|---|
| 최고관리자 | `admin` | 라이브러리 등록·승인, 플랫폼 전체 설정 |
| 그룹 관리자 | `demo-secadmin` | 워크플로우 설계, 상위 알림 확인 |
| 내부 승인자 | `demo-approver1` | 원래 승인자의 결재함 확인 |
| 대리 결재자 | `demo-delegate1` | 위임받은 결재 처리 |
| 신청자 | `demo-requester1` | 배포된 워크플로우 요청과 결과 확인 |

데모 계정 비밀번호는 로컬 전용 파일에서 확인한다.

```bash
jq -r '.accounts | to_entries[] | "\(.key)\t\(.value)"' .env.demo-access.json
```

로그인 전환을 빠르게 하려면 일반 창, 시크릿 창, 별도 브라우저 프로필을 나누어 사용한다. Mailpit 메일은 초기화할 때 삭제되지 않으므로 항상 가장 최근 요청의 메일을 연다.

## 2. 시연 목록

| 순서 | 시연 | 보여줄 핵심 | 예상 시간 |
|---|---|---|---|
| 1 | 대리 결재와 기한 알림 | 결재 위임, 원래 승인자 추적, 기한 독촉과 상위 알림 | 전체 4분 이상, 빠른 시연 2분 |
| 2 | 승인된 JS 라이브러리 | 관리자 승인, 정확한 버전 고정, JS 노드 재사용, 실행 중 npm 미접속 | 3분 |

각 시연은 독립적으로 실행할 수 있다. 여러 번 연습한 뒤에는 다시 `pnpm demo:reset`을 실행한다.

## 3. 시연 1 — 대리 결재와 기한 알림

### 준비된 데이터

- 워크플로우: `실습 3 · 대리 결재와 기한 알림`
- 신청자: `demo-requester1`
- 원래 승인자: `demo-approver1`
- 대리 결재자: `demo-delegate1`
- 그룹 관리자: `demo-secadmin`
- 처리 기한: 1분
- 상위 알림 유예 시간: 1분

### 시연 순서

1. `demo-requester1`로 로그인한다.
2. **요청하기**에서 `실습 3 · 대리 결재와 기한 알림`을 선택한다.
3. 사번 `E-1001`, 권한 `read`, 대상 시스템 `개발 포털`을 입력하고 요청한다.
4. **내 요청**에서 실행이 승인 대기 상태인지 확인한다.
5. `demo-approver1`로 로그인해 새 결재가 원래 승인자의 결재함에 나타나지 않는지 확인한다.
6. `demo-delegate1`로 로그인한다.
7. **내 결재함**에서 `대리 결재` 배지와 원래 승인자 `demo-approver1`이 표시되는지 확인한다.
8. 바로 승인하지 않고 1분 이상 기다린다.
9. Mailpit에서 `demo-delegate1@pxm.local`로 발송된 기한 독촉 메일을 확인한다.
10. 다시 1분 정도 기다린 뒤 `demo-secadmin@pxm.local` 대상 상위 알림을 확인한다.
11. 대리 결재자 화면에서 요청을 승인한다.
12. 신청자 화면으로 돌아가 워크플로우가 완료됐는지 확인한다.

### 설명할 내용

- 대리 결재는 같은 그룹의 활성 사용자에게 기간과 워크플로우 범위를 정해 설정한다.
- 현재 위임은 이 실습 워크플로우에만 적용된다.
- 담당자가 바뀌어도 원래 승인자, 대리 결재자와 처리 이력이 함께 남는다.
- 처리 기한이 지나면 현재 담당자에게 먼저 알리고, 계속 처리되지 않으면 그룹 관리자에게 알린다.
- 예정하지 못한 부재에는 관리자가 특정 미결 결재만 긴급 재배정할 수도 있다.

### 빠른 시연

시간이 부족하면 기한 알림 대기를 생략한다. 신청 → 원래 승인자의 결재함에 없음 → 대리 결재자의 배지와 원래 승인자 확인 → 승인 → 완료 순서만 보여준다. 기한 알림은 Mailpit에 남은 최근 메일로 설명한다.

## 4. 시연 2 — 승인된 JS 라이브러리

### 준비된 데이터

- 워크플로우: `실습 4 · 승인된 JS 라이브러리 사용`
- 승인 라이브러리: `lodash@4.17.21`
- 승인 범위: `데모 · 보안운영팀`
- 실행 프리셋: `lodash 숫자 집계`
- 입력값: `12, 7, 12, 30, 7`

### 시연 순서

1. `admin`으로 로그인해 **플랫폼 설정 → JS 라이브러리**를 연다.
2. `lodash@4.17.21`이 **사용 승인** 상태이고 사용 범위가 `데모 · 보안운영팀`인지 확인한다.
3. 패키지 이름에 `dayjs`를 입력하고 **최신 버전**을 누른다.
4. `latest` 문자열 대신 조회 시점의 정확한 버전이 자동으로 입력되는 것을 보여준다. 이번 시연에서는 **준비**를 누르지 않아도 된다.
5. `demo-secadmin`으로 로그인해 **워크플로우 설계**에서 `실습 4 · 승인된 JS 라이브러리 사용`을 연다.
6. `lodash로 숫자 집계` JS 노드를 선택한다.
7. 승인된 `lodash@4.17.21`이 연결돼 있고 코드가 `libs['lodash']`로 라이브러리를 사용하는지 확인한다.
8. `demo-requester1`로 로그인해 **요청하기**에서 같은 워크플로우를 선택한다.
9. 프리셋 `lodash 숫자 집계`를 적용하고 실행한다.
10. 실행 결과에서 아래 값이 나오는지 확인한다.

```json
{
  "count": 5,
  "unique": [12, 7, 30],
  "sortedUnique": [7, 12, 30],
  "total": 68
}
```

### 설명할 내용

- 최고관리자가 npm 패키지와 버전을 먼저 준비하고 그룹별 사용 범위를 승인한다.
- 설치 스크립트는 실행하지 않으며 브라우저 번들 가능 여부, 크기와 해시를 검사한다.
- `latest`는 편의를 위한 조회 기능이며 실제 저장과 배포에는 `4.17.21` 같은 정확한 버전이 고정된다.
- JS 노드는 임의의 `require`나 `import`를 실행하지 않고 승인된 라이브러리만 `libs` 객체로 받는다.
- 실행할 때 npm에 접속하지 않는다. 관리자가 미리 검증한 번들을 DB와 워크플로우 배포 버전에서 사용한다.
- 승인 후 npm의 최신 버전이 바뀌거나 원본 패키지가 삭제돼도 기존 배포 버전은 같은 코드로 실행된다.

### 문제가 생겼을 때 확인할 것

- 라이브러리가 JS 노드 목록에 없으면 워크플로우 그룹과 라이브러리 승인 범위를 확인한다.
- 저장·배포가 거부되면 라이브러리가 **사용 중지** 상태인지 확인한다.
- 최신 버전 조회가 실패하면 API 서버에서 npm 레지스트리에 접근할 수 있는지 확인한다.
- 실행 결과가 남아 다음 연습에 방해되면 진행 중 실행이 없는지 확인한 뒤 `pnpm demo:reset`을 실행한다.

## 5. 시연 후 검증과 복구

현재 서버와 데이터로 전체 데모 경로를 점검하려면 다음 명령을 실행한다.

```bash
pnpm demo:check
```

발표 전 독립 환경에서 서버 기동부터 브라우저 동선까지 검사하려면 다음 명령을 사용한다.

```bash
pnpm demo:verify
```

연습이 끝나면 기준 상태로 복구한다.

```bash
pnpm demo:reset
```
