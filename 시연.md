# PXM 7월 6일 회의 안건 기준 시연 가이드

이 문서는 PXM을 처음 다루는 발표자도 그대로 따라 할 수 있도록 작성했다. 발표 순서는 **2026년 7월 6일 회의 안건 순서**를 기준으로 하며, 7월 7일 이후 추가된 웹 기능과 UX는 가장 자연스러운 안건의 시나리오 안에 함께 배치했다.

## 0. 발표 중에는 이 부분만 본다

아래는 발표자용 빠른 진행본이다. **발표 중에는 0장만 보고 진행**하고, 입력값이나 장애 대응이 필요할 때만 뒤의 상세 시나리오를 찾는다.

- **15분 발표:** 장면 `1 → 2 → 7 → 12`
- **회의 안건 전체 확인:** 장면 `1 → 13`
- **API 결재 중심 발표:** 장면 `7 → 11`
- **결재 기능 중심 발표:** 장면 `11 → 12 → 13`

표시 방법은 모든 장면에서 같다.

- `계정` — 사용할 브라우저 프로필
- `이동` — 사이드바에서 누를 메뉴
- `조작` — 실제로 클릭할 것
- `확인` — 청중에게 보여줄 화면
- `멘트` — 그대로 읽어도 되는 한 문장

### 시작 전 1분 점검

- PXM `5174`, API Playground `5175`, Mailpit `8025`가 열리는지 확인한다.
- 브라우저 프로필에 `admin`, `demo-secadmin`, `demo-requester1`, `demo-approver1`, `demo-approver2`, `demo-delegate1`을 미리 로그인한다.
- `실습 5`, `실습 4`, `실습 7`이 목록에 보이는지 확인한다.
- `실습 9`가 목록에 있고 SSH 노드의 **연결 테스트가 성공**하는지 확인한다. 없으면 SSH 자격증명을 만들고 `pnpm demo:seed`.
- 발표 중 디자이너에서 만든 변경은 **저장하지 않는다**.
- `dayjs`는 최신 버전 조회까지만 하고 **준비하지 않는다**.
- `데모 · 보안운영팀`은 **삭제하지 않는다**.
- 장면 7에서 만든 그룹·사용자·서비스 계정·키는 그 장면 안에서 **삭제 또는 비활성화까지 하고 넘어간다**.

### 장면 1 · 동시에 여러 워크플로우 보기

- `계정` `demo-secadmin`
- `이동` **워크플로우 설계**
- `조작` **더 보기 → 불러오기**에서 `실습 5`, 다시 `실습 4`를 차례로 연다.
- `확인` 상단에 두 탭이 동시에 있고 클릭할 때 설계가 바로 바뀐다.
- `조작` `실습 5`의 빈 캔버스를 우클릭해 **모두 선택** → 상단 **선택 노드 복사** → `+` 새 탭 → **붙여넣기**를 누른다.
- `조작` 빈 캔버스, 노드, 연결선을 각각 우클릭해 서로 다른 메뉴를 보여준다.
- `확인` 노드 추가·복사·복제, 연결 사이에 노드 추가, 분기 설정 메뉴가 보인다.
- `멘트` “여러 설계를 탭으로 동시에 비교하고 일부 흐름을 다른 워크플로우에 그대로 재사용할 수 있습니다.”
- `마무리` 새 탭은 저장하지 않고 닫는다.

### 장면 2 · Export 버전 정보

- `계정` `demo-secadmin`
- `이동` **워크플로우 관리 → 실습 5 · 디자이너와 배포 수명주기**
- `확인` 우측의 **Version / 배포 버전 / 운영 상태**를 보여준다. 두 버전이 같으면 “현재 저장본과 배포본이 일치합니다”라고 말한다.
- `확인` **메타데이터 수정**을 열어 변경 메모가 Version Note와 Export 파일에 기록된다는 안내를 보여주고 취소한다.
- `조작` **버전 이력**을 누른다. 이전 버전이 있으면 **비교**를 눌러 변경 경로를 보여준다.
- `선택 시연` 이전 버전의 **롤백**을 누르고 확인창의 “새 버전이 만들어지고 기존 버전은 남는다”는 문구만 보여준 뒤 취소한다.
- `조작` Drawer를 닫고 **파일로 내보내기**를 누른다.
- `확인` 내려받은 JSON에서 `workflow.definition_id`, `workflow.version`, `workflow.exported_version_note`, `plugin_dependencies`, `security.secrets_policy: redacted`를 차례로 찾는다.
- `조작` 목록에서 `실습 4 · 승인된 JS 라이브러리 사용`을 선택해 **파일로 내보내기**를 누르고 `script_library_dependencies`의 `lodash`와 고정 버전 `4.17.21`을 확인한다.
- `이동` 목록에서 다시 `실습 5` 선택
- `확인` 화면의 **Template ID / Version**이 방금 Export한 `definition_id / version`과 같은지 확인한 다음, **작성 이력**의 생성자·최근 수정자·시각을 보여준다.
- `주의` 작성 이력은 워크플로우 전체의 생성자와 최근 수정자다. 각 버전의 작성자나 배포자를 보여주는 화면이라고 설명하지 않는다.
- `멘트` “이 파일이 어느 워크플로우의 몇 버전인지 다시 대조할 수 있고, 플러그인과 승인된 JS 라이브러리 버전 및 Secret 제거 정책도 함께 전달됩니다.”

### 장면 3 · 명령 실행 터미널

- `계정` `admin` → `demo-secadmin`
- `이동` `admin`: **명령어 관리**, `demo-secadmin`: **워크플로우 설계 → 실습 7**
- `확인` `builtin.echo`의 허용 인수·시간·출력 제한을 보여준다.
- `조작` `실습 7`을 실행하고 완료된 명령 노드를 누른다.
- `확인` 터미널의 명령, stdout/stderr, 종료 코드, 소요 시간과 복사 버튼을 보여준다.
- `멘트` “임의 셸이 아니라 관리자가 허용한 명령만 실행하고 결과를 감사 가능한 터미널 형태로 남깁니다.”

### 장면 4 · SSH 원격 실행

- `계정` `demo-secadmin`
- `사전` 발표 전에 **SSH 자격증명을 만들고 `pnpm demo:seed`**를 실행해 실습 9를 준비해 둔다.
- `이동` **연동 자격증명 → 신규 → Type `SSH`**
- `확인` Host, Port, Username, Password/Private Key 입력 구성
- `조작` **서버 키 가져오기**를 눌러 `SHA256:...` 지문이 채워지는 것을 보여준 뒤 취소한다.
- `이동` **워크플로우 설계 → 실습 9 · SSH 원격 명령 실행**
- `확인` SSH 노드의 자격증명 선택과 Command, `{{formData.target_path}}` 바인딩
- `조작` **실행** → `확인할 원격 경로`를 `/tmp`로 두고 제출한다.
- `확인` SSH 노드 출력의 `exit_code: 0`과 stdout, 다음 JS 노드의 `hostname / kernel / entryCount`
- `멘트` “서버 암호뿐 아니라 접속 대상의 Host Key까지 고정해 중간자 접속을 막고, 원격 명령 결과는 후속 노드가 쓰는 데이터로 남깁니다.”

### 장면 5 · 승인된 JS module

- `계정` `admin` → `demo-secadmin` → `demo-requester1`
- `이동` `admin`: **JS 라이브러리**
- `확인` `lodash@4.17.21`의 승인 상태·해시·허용 그룹을 보여준다.
- `조작` `dayjs` 입력 → **최신 버전 조회**까지만 누른다.
- `이동` `demo-secadmin`: **워크플로우 설계 → 실습 4**
- `확인` JS 노드의 `lodash@4.17.21`과 `libs['lodash']`를 보여준다.
- `이동` `demo-requester1`: **요청하기 → 실습 4 → lodash 숫자 집계 → 요청 제출**
- `확인` **내 요청** 결과의 `total: 68`을 보여준다.
- `멘트` “실행 중 npm에서 내려받지 않고 관리자가 승인한 정확한 버전과 해시만 사용합니다.”

### 장면 6 · 그룹 권한·이력·삭제 영향

- `계정` `admin`
- `이동` **사용자 및 권한 → 데모 · 보안운영팀**
- `확인` 그룹 멤버, 서비스 계정, API Key, 외부 승인자 매핑 탭을 차례로 보여준다.
- `조작` **삭제 영향 확인**을 누른다.
- `확인` 포함 워크플로우와 API Key, 사용 이력 때문에 **복구 가능한 삭제**가 적용되는 것을 보여준다.
- `마무리` 반드시 취소한다.
- `멘트` “그룹 삭제 시 워크플로우와 키는 함께 비활성화하고, 과거 이력은 추적을 위해 보존합니다.”

### 장면 7 · 그룹·키를 현장에서 만들고 사용 → 이력 → 차단까지

준비된 키 화면을 설명하지 말고 **직접 만들어서 쓰고 막히는 것까지** 한 번에 보여준다. 11분.

- `계정` `admin` → `demo-live-manager`(여기서 생성) + API Playground
- `① 그룹` `admin`: **사용자 및 권한 → 새 그룹**으로 `발표 · 임시 검증 그룹` 생성 → **삭제 영향 확인** → 사용 이력이 없으므로 **영구 삭제**
- `② 사용자` `admin`: `데모 · 보안운영팀`에 `demo-live-manager / PxmDemo!2026`을 **그룹 관리자**로 생성하고 다른 브라우저 프로필로 로그인
- `③ 서비스 계정 · 키` `demo-live-manager`: `demo-live-client` 서비스 계정 생성 → `발표 · 현장 발급 키` 발급(조회+실행, `실습 4`만, 분당 60). 원문 복사
- `확인` 소유자는 서비스 계정, 발급자 ID는 `demo-live-manager`로 따로 표시된다. `결재 승인·반려`는 서비스 계정이라 **선택 자체가 막혀 있다**
- `④ 사용` Playground에 새 키로 연결 → `실습 4`만 보인다 → 실행 → Result의 `total: 45` 확인
- `⑤ 이력` PXM **API Key → 사용 이력**에서 초록색 성공 행을 연다. 소유자, Endpoint, Request ID, IP, Business Actor를 짚고 **실행 모니터링**으로 같은 실행을 연다
- `⑥ 차단` Playground를 `데모 · API 결재 키`로 다시 연결한다. 목록이 `실습 1`로 통째로 바뀌고, 실행을 누르면 빨간 `HTTP 403 · MISSING_SCOPE`가 뜬다
- `⑥-1 조회는 된다` **API Console**을 열어 `GET /templates` `200` 바로 아래 `POST .../execute` `403`을 나란히 보여준다. 같은 키로 조회는 통과하고 실행만 막혔다
- `⑦ 이력` 사용 이력을 새로고침한다. 같은 화면에 빨간 `API 요청 실패 · 403` 행이 **결재 키 소유자 이름으로** 남는다. **감사 로그**에는 `api_key.issued` 주체가 `demo-live-manager`로 남는다
- `마무리` `발표 · 현장 발급 키`를 **비활성화**하고 Playground에서 재연결해 `401`을 보여준다
- `멘트` “지금 만든 관리자와 서비스 계정으로 키를 발급했고, 그 키의 실제 호출과 권한 없는 호출이 막힌 기록까지 같은 그룹 경계 안에서 추적했습니다.”
- `입력값` 정확한 값과 실패 유형별 응답은 [9장](#9-회의-안건-7과-12--그룹부터-api-key까지-현장에서-만들고-사용-이력과-차단-확인)을 본다.

### 장면 8 · 워크플로우 생성자

- `계정` `demo-secadmin`
- `이동` **워크플로우 관리 → 실습 5**
- `확인` 우측 **작성 이력**의 생성자, 최근 수정자, 시각을 보여준다.
- `조작` 같은 상세 패널의 **버전 이력**을 누른다.
- `확인` 버전 번호·저장 시각과 Version Note를 보여준다.
- `멘트` “워크플로우 전체의 생성자와 최근 수정자를 확인하고, 별도의 버전 목록에서 저장 시각과 변경 메모를 확인할 수 있습니다.”
- `주의` 이 화면만으로 “누가 각 버전을 저장하거나 배포했는지”까지 확인된다고 말하지 않는다. 그 주체는 감사 로그로 확인한다.

### 장면 9 · 최고관리자와 그룹 관리자

- `계정` `admin` → `demo-secadmin` → `demo-requester1`
- `확인` `admin`에는 새 그룹·플랫폼 운영 메뉴가 보인다.
- `조작` 사이드바를 접고 아이콘에 마우스를 올려 메뉴 툴팁을 보여준다.
- `확인` `demo-secadmin`은 받은 그룹 안의 멤버·키·워크플로우만 관리한다.
- `확인` `demo-requester1`은 요청하기·내 요청·내 결재함만 본다.
- `멘트` “역할에 따라 메뉴만 다른 것이 아니라 API의 그룹·리소스 권한도 동일하게 제한됩니다.”

### 장면 10 · 파라미터 세트를 만들고 그걸로 실행

- `계정` `demo-secadmin` → `demo-requester1`
- `이동` `demo-secadmin`: **실행 프리셋**
- `확인` 세트별 대상 워크플로우·alias·입력 키. `실습 2`에 위험도가 다른 두 세트가 있다.
- `조작` **새 프리셋** → 대상 `실습 2`, 이름 `발표 · 현장 파라미터 세트`, alias `demo-live-preset`, 범위 `소유 그룹`
- `조작` 입력값 JSON에 `{"emp_id":"E-1001","privilege_level":"read","target_system":"발표 데모 포털"}`을 넣고 저장한다.
- `이동` `demo-requester1`: **요청하기 → 실습 2**
- `조작` 폼 위 **파라미터 세트**에서 방금 만든 세트를 누른다. 값이 자동으로 채워진다.
- `조작` **요청 제출**
- `확인` **내 요청**에서 결재 없이 완료되고 대상 시스템이 `발표 데모 포털`로 찍힌다.
- `멘트` “관리자가 검증한 조합을 세트로 등록해 두면 신청자는 고르기만 하면 됩니다. 같은 세트를 외부 시스템은 alias 하나로 부릅니다.”
- `마무리` alias가 `demo-`로 시작해 다음 초기화 때 자동 정리된다.

### 장면 11 · API로 결재까지 처리

- `계정` 없음. API Playground 사용
- `조작` `trigger` 키로 `실습 1` 실행 → Instances에서 결재 대기 확인
- `조작` `approver` 키로 다시 연결 → Approval History → 상세 → 승인
- `조작` `trigger` 키로 돌아가 같은 Instance의 `COMPLETED` 결과 확인
- `확인` API Console의 Idempotency-Key, Request ID, cURL을 보여준다.
- `멘트` “실행 주체와 결재 주체의 키를 분리한 상태로 실행·조회·결재·결과 확인을 API만으로 끝냅니다.”

### 장면 12 · 다단계 공동 결재와 Webhook

- `계정` `demo-requester1` → `demo-approver1` → `demo-secadmin` → `demo-approver2` → `demo-secadmin` → `admin`
- `조작` **요청하기 → 실습 6**에서 `공동 결재 데모 요청` 프리셋으로 실행한다. 반복 실습이면 외부 요청 번호 끝에 현재 시각을 붙인다.
- `조작` `demo-approver1`이 1단계 결재를 **보류**하고, 이력에 보류 의견이 남는 것을 확인한 뒤 다시 **승인**한다.
- `확인` `demo-approver1` 한 명만 승인해도 2단계로 넘어가지 않고 `demo-approver2`의 결재가 남아 있다. 이것이 `ALL`이다.
- `조작` `demo-secadmin`: **실행 모니터링**에서 해당 실행을 **일시중지 → 재개**한다.
- `조작` `demo-approver2`가 승인하고, 2단계에서 `demo-secadmin`이 승인한다.
- `확인` 2단계의 두 후보 중 한 명의 승인만으로 완료되고 나머지는 취소된다. 이것이 `ANY`다.
- `이동` `admin`: **결과 Webhook** → `데모 · 결재 결과 수신`
- `확인` 상태 `전송 완료`, HTTP `200`, 시도 이력, 실행 ID와 최종 승인 결과
- `멘트` “여러 명이 모두 승인해야 하는 ALL과 한 명만 승인하면 되는 ANY를 연결하고, 최종 결과는 외부 시스템에 Webhook으로 전달합니다.”

### 장면 13 · 대리 결재와 기한 독촉·상위 알림

- `계정` `demo-requester1` → `demo-approver1` → `demo-delegate1` → `admin`
- `조작` **요청하기 → 실습 3**에서 `E-1001 / read / 개발 포털`로 실행한다.
- `확인` 원래 승인자 `demo-approver1`의 결재함에는 보이지 않는다.
- `확인` `demo-delegate1`의 결재함에서 **대리 결재** 배지, 원래 승인자 `demo-approver1`, 위임 기한을 본다.
- `조작` 상단 **대리 결재 설정**을 열어 `demo-approver1 → demo-delegate1`, 적용 워크플로우, `기존 미결 포함`, 설정자와 설정 사유를 확인한다.
- `대기` 실행 후 1분이 지나면 **승인 알림**에 `기한 독촉 → demo-delegate1`이 생기고 Mailpit에도 메일이 도착한다.
- `대기` 추가 1분 후 `상위 알림 → demo-secadmin`을 확인한다.
- `조작` `demo-delegate1`이 승인한다.
- `확인` 처리 이력에 원래 승인자와 실제 처리자가 따로 남고 요청이 완료된다.
- `멘트` “휴가나 부재 중에는 승인 권한을 기간과 워크플로우 범위로 위임하고, 느려지면 대리자 독촉과 그룹 관리자 상위 알림으로 이어집니다.”

### 발표 중 길을 잃었을 때

1. 현재 계정을 먼저 확인한다.
2. 사이드바 메뉴 이름이 다르면 다른 역할로 로그인한 것이다.
3. 디자이너에서 화면이 복잡해지면 `Esc`를 누르고 **전체 화면에 맞춤**을 누른다.
4. API 결과가 아직 없으면 Instances 또는 사용 이력에서 **새로고침**한다.
5. API Key 원문을 잃었으면 찾으려 하지 말고 새로 발급한다.
6. 데이터가 꼬였으면 진행 중 실행을 종료한 뒤 `pnpm demo:reset`으로 돌아간다.

> **발표자용 빠른 진행본은 여기까지다. 아래 내용은 리허설·입력값·질의응답용 상세 참고 자료다.**

## 1. 발표 원칙과 권장 순서

- 웹에서 사용자가 직접 확인할 수 있는 기능만 시연한다.
- 내부 구현 안정화는 화면 결과를 뒷받침하는 설명으로만 짧게 언급한다.
- 디자이너 기능만 길게 보여주지 않고 신청자, 승인자, 그룹 관리자, 외부 API 사용자의 업무 흐름을 함께 보여준다.
- 실제 생성·발급이 필요한 기능은 화면을 설명하지 말고 그 자리에서 만들어 보여준다. 그룹·사용자·서비스 계정·API Key는 9장에서 한 번에 수행한다.
- 시간이 부족하면 `핵심` 표시 장면만 진행한다.

| 순서 | 7월 6일 회의 안건 | 시연 | 권장 시간 |
|---:|---|---|---:|
| 1 | 동시에 여러 워크플로우 보기 | 핵심 | 3분 |
| 2 | Export할 때 버전 정보 | 핵심 | 2분 |
| 3 | 명령어 노드 터미널 화면 | 핵심 | 2분 |
| 4 | SSH 플러그인 | 핵심 | 4분 |
| 5 | JS module import | 핵심 | 3분 |
| 6 | 그룹별 권한·이력과 그룹 삭제 | 핵심 | 4분 |
| 7 | 현장 생성 + API Key 사용·이력·차단 (안건 12 포함) | 핵심 | 11분 |
| 8 | 워크플로우 생성자 | 포함 | 1분 |
| 9 | 최고관리자와 그룹 관리자 역할 | 포함 | 2분 |
| 10 | 자주 쓰는 파라미터 세트 | 핵심 | 5분 |
| 11 | 웹 백엔드 JS 보안 | 발표 제외 | 설명만 |
| 12 | 현장 생성: 그룹→사용자→키→API→이력 | 7번에 통합 | — |
| 13 | API 실행→결재 조회→API 승인→결과 | 선택 | 5분 |

전체 진행 시 약 42분이다. 15분 발표는 `1 → 2 → 5 → 7` 순서를 권장한다. 결재가 발표의 핵심이면 `5` 대신 `13`을 진행한다.

안건 7과 12는 발급·호출·이력 화면이 그대로 겹쳐 한 장면으로 합쳤다. 9장에서 그룹 생성부터 키 폐기까지 한 번에 진행한다.

## 2. 발표 전 준비

### 2-1. 기준 데이터 초기화

프로젝트 루트에서 실행한다.

```bash
pnpm demo:reset
```

초기화되는 발표 데이터:

- `데모 · 보안운영팀` 그룹과 데모 계정
- 실습 워크플로우와 입력 프리셋. `실습 9`(SSH)는 그룹에 SSH 자격증명이 있을 때만 만들어진다
- 승인된 `lodash@4.17.21`
- 명령 실행 허용 목록과 모의 외부 서비스 연동 데이터
- 외부 승인자 매핑, 위임 설정, 결과 Webhook
- API 실행용 서비스 계정과 실행·결재 API Key
- 이전 리허설에서 직접 만든 `demo-live-*` 사용자·서비스 계정과 `발표 · 현장 발급 키` 및 그 호출 이력

진행 중인 데모 실행이 있으면 데이터 보호를 위해 초기화가 중단된다. 해당 실행을 완료하거나 종료한 뒤 다시 실행한다.

### 2-2. 필요한 서버

```bash
pnpm demo:service
pnpm dev:api-playground
```

| 화면 | 로컬 주소 | 현재 발표 PC의 같은 망 주소 |
|---|---|---|
| PXM 웹 | http://localhost:5174 | http://192.168.136.146:5174 |
| API Playground | http://localhost:5175 | http://192.168.136.146:5175 |
| Mailpit | http://localhost:8025 | http://192.168.136.146:8025 |

IP가 바뀌었다면 `ip addr`로 확인한 현재 IP를 사용한다.

### 2-3. 로그인과 API Key 확인

기본 최고관리자는 `admin / admin1234`다. 환경에서 `PXM_BOOTSTRAP_ADMIN_PASSWORD`를 변경했다면 변경한 값을 사용한다.

```bash
jq -r '.accounts | to_entries[] | "\(.key)\t\(.value)"' .env.demo-access.json
jq -r '.api_keys | to_entries[] | "\(.key)\t\(.value.api_key)"' .env.demo-access.json
```

| 계정 | 비밀번호 | 역할 | 주요 시연 |
|---|---|---|---|
| `admin` | `admin1234` | 최고관리자 | 그룹·사용자 생성, 플랫폼 설정, JS 라이브러리, 명령어 관리 |
| `demo-secadmin` | `55ymZr70Y6VmZvA1WMimyLwb` | 그룹 관리자 | 워크플로우, API Key, 실행 프리셋, 그룹 범위 이력 |
| `demo-requester1` | `Lz0B5LjwG_ksrcMTd_enzunZ` | 신청자 | 파라미터 세트 적용과 내 요청 |
| `demo-approver1` | `cP87qSuVpDe54OO_U76CFIsF` | 승인자 | 웹·API 결재 |
| `demo-approver2` | `XqCE4222FRpJX9q8TmmPxcJV` | 공동 승인자 | ALL 결재 |
| `demo-delegate1` | `rWX_F0sH4CowIzgsjkZ6Dcct` | 대리 승인자 | 위임 결재 |

발표 중 9장에서 직접 만드는 계정은 `demo-live-manager / PxmDemo!2026`이다. 리셋하면 지워진다.

**주의:** 위 비밀번호는 이 발표 PC의 `.env.demo-access.json` 기준이다. 그 파일을 지우고 초기화하면 전부 새 값으로 바뀌므로 아래 명령으로 다시 확인한다. **화면 공유 중에는 이 표를 띄우지 않는다.**

발표 전에 일반 창, 시크릿 창, 별도 브라우저 프로필로 계정을 미리 로그인해 둔다. API Key 원문은 화면 공유 전에 메모장에 노출하지 말고 `.env.demo-access.json`에서 복사한다.

---

## 3. 회의 안건 1 — 동시에 여러 워크플로우 보기

**목표:** 여러 설계를 동시에 열어 비교하고, 다른 워크플로우의 일부를 복사해 재사용할 수 있음을 보여준다.

**로그인:** `demo-secadmin`
**사용 데이터:** `실습 5 · 디자이너와 배포 수명주기`, `실습 4 · 승인된 JS 라이브러리 사용`

1. **워크플로우 설계**로 이동한다.
2. **더 보기 → 불러오기**에서 `실습 5 · 디자이너와 배포 수명주기`를 연다.
3. 다시 **더 보기 → 불러오기**에서 `실습 4 · 승인된 JS 라이브러리 사용`을 연다.
4. 상단에 두 워크플로우가 각각 탭으로 유지되는 것을 보여주고 `실습 5 ↔ 실습 4`를 두 번 오간다.
5. `실습 5`에서 캔버스 빈 곳을 우클릭한다. **노드 추가, 붙여넣기, 모두 선택, 화면 맞춤** 메뉴를 보여준다.
6. 캔버스를 다시 우클릭해 **모두 선택**한 뒤 상단의 **선택 노드 복사**를 누른다.
7. `+`로 새 워크플로우 탭을 만들고 **복사한 노드 붙여넣기**를 누른다. 노드와 내부 연결이 새 탭으로 함께 복사되는 것을 보여준다.
8. 새 탭은 저장하지 않고 닫는다. 원본 두 탭은 그대로 유지됨을 확인한다.
9. `실습 5`에서 노드 하나를 우클릭해 **속성 열기 / 복사 / 복제 / 삭제**를 보여주고, 연결선을 우클릭해 **사이에 노드 추가 / 분기 설정 / 연결 삭제**를 보여준다. 실제 삭제는 하지 않는다.
10. 빈 캔버스를 더블클릭하거나 `Tab`을 눌러 빠른 노드 검색을 열고 `Timer`를 검색한다. 추가는 취소해도 된다.
11. **더 보기 → 자동 정렬**을 실행한 뒤 캔버스 우클릭 → **자동 정렬 되돌리기**로 복구한다.
12. **캔버스 넓게 보기**를 켜 좌우 패널이 한 번에 접히는 것을 보여주고 `Esc`로 돌아온다.

같이 보여주는 7월 6일 이후 UX:

- 다중 워크플로우 탭과 빈 탭 교체 규칙
- 워크플로우 간 노드·연결 복사
- 캔버스·노드·연결선 우클릭 컨텍스트 메뉴
- 빠른 노드 추가와 검색
- 자동 정렬과 한 번 되돌리기
- 캔버스 넓게 보기
- 노드 카드의 핵심 설정 요약, 분기 라벨, 미니맵 유형 색상

발표 멘트: “설계를 하나 닫고 다른 설계를 여는 방식이 아니라, 필요한 흐름을 탭으로 동시에 유지하고 일부 구성을 다른 워크플로우에 재사용할 수 있습니다.”

성공 기준: 상단에 두 워크플로우 탭이 동시에 보이고, 임시 탭에 복사한 노드가 나타난다.

## 4. 회의 안건 2 — 워크플로우 Export할 때 버전 정보

**목표:** 내보낸 파일만으로 어떤 설계 버전과 의존성을 전달했는지 확인할 수 있음을 보여준다.

**로그인:** `demo-secadmin`
**사용 데이터:** `실습 5 · 디자이너와 배포 수명주기`

### 4-1. 현재 저장 버전과 배포 버전 확인

1. **워크플로우 관리**로 이동해 `실습 5`를 선택한다.
2. 우측 상세의 값을 가리킨다.
   - `Version`: 가장 최근에 **저장된 설계 버전**
   - `배포 버전`: API·수동 실행에서 실제 사용하는 **배포 버전**
   - `운영 상태`: 배포됨, 미배포 변경 또는 배포 중지
3. 두 버전이 같으면 “현재 저장본과 배포본이 일치합니다”라고 말한다. 숫자가 다를 때만 “저장과 배포가 분리되어 있습니다”라고 설명한다.
4. **메타데이터 수정**을 눌러 이름·설명·태그·관리 그룹과 필수 **변경 메모** 필드를 보여준다. “새 버전으로 저장”은 누르지 않고 취소한다.
5. 상세의 `배포 / 배포 중지 / 재활성화` 버튼을 가리킨다. 설계 화면의 저장과 운영 배포가 별도 화면과 권한으로 관리된다고 설명하고, 발표 중에는 상태를 바꾸지 않는다.

### 4-2. 버전 비교와 안전한 롤백 확인

1. `실습 5` 상세의 **버전 이력**을 누른다.
2. 우측 Drawer가 열리는 것을 확인한다.
3. 버전 번호, 저장 시각, Version Note를 보여준다.
4. 이전 버전이 있으면 그 행의 **비교**를 눌러 `added / changed / removed`와 변경 경로를 보여준다.
5. 이전 버전의 **롤백**을 누른다. 확인창에 “v숫자 내용으로 새 버전이 만들어집니다. 기존 버전은 그대로 남습니다”가 표시되는 것만 보여주고 **취소**한다.
6. 이전 버전이 하나도 없으면 비교·롤백은 건너뛰고 “두 번째 저장부터 비교와 롤백이 가능합니다”라고 말한다. 없는 변경 이력을 있는 것처럼 설명하지 않는다.

### 4-3. Export 파일에서 실제 값 확인

1. 버전 Drawer를 닫고 `실습 5` 상세로 돌아온다.
2. **파일로 내보내기**를 눌러 `.pxm-workflow.json` 파일을 다운로드한다.
3. 다운로드한 파일을 브라우저 새 탭이나 텍스트 편집기로 열어 다음 값을 차례로 검색한다.

```text
schema_version
exported_at
workflow.definition_id
workflow.version
workflow.exported_version_note
workflow.plugin_dependencies
security.secrets_policy = redacted
security.redacted_paths
```

4. `plugin_dependencies`에서 `connector.db.mongodb.query`, `builtin.http_request`와 각 플러그인 버전을 가리킨다.
5. `security.secrets_policy`가 `redacted`인지 확인한다. Secret 원문 대신 자격증명 참조만 전달된다고 설명한다.
6. 승인된 JS 라이브러리 의존성은 `실습 5`가 아니라 관리 목록의 `실습 4 · 승인된 JS 라이브러리 사용`을 선택해 별도로 Export한다.
7. `실습 4` Export 파일의 `workflow.script_library_dependencies`에서 `lodash`, `4.17.21`, 사용하는 노드 ID를 확인한다.

### 4-4. Export 파일과 웹의 워크플로우가 같은 대상인지 대조

1. 관리 목록에서 다시 `실습 5`를 선택한다.
2. 우측의 **Template ID**와 Export 파일의 `workflow.definition_id`가 같은지 확인한다.
3. 우측의 **Version**과 Export 파일의 `workflow.version`이 같은지 확인한다.
4. 그 아래 **작성 이력**에서 생성자, 최근 수정자, 생성·수정 시각을 보여준다.
5. 다음처럼 설명한다.

> “ID와 버전으로 이 Export 파일이 어느 워크플로우의 어떤 저장본인지 대조할 수 있습니다. 작성 이력은 이 워크플로우 전체를 누가 만들었고 마지막으로 수정했는지를 보여줍니다.”

**중요:** 현재 작성 이력은 버전별 작성자나 배포자를 보여주는 화면이 아니다. 따라서 “이 버전을 이 사용자가 만들었다”라고 설명하면 안 된다. 버전별 저장·배포·롤백 주체는 감사 로그에서 확인한다.

### 4-5. 기능별로 어디를 보여주는지

| 보여줄 기능 | 실제로 조작할 곳 | 화면에서 확인할 증거 |
|---|---|---|
| 저장과 배포 분리 | 설계 화면의 **저장**, 관리 상세의 **배포/배포 중지** | 설계 저장과 운영 상태 변경이 별도 화면으로 관리됨 |
| 현재 버전과 실제 배포 버전 | 워크플로우 관리 상세 | `Version`, `배포 버전`, `운영 상태` |
| 버전 비교 | 관리 상세 → **버전 이력 → 비교** | 변경 유형과 JSON 경로 |
| 롤백 시 새 버전 생성 | 이전 버전 → **롤백** | 확인창의 “새 버전 생성, 기존 버전 유지” 문구; 실행은 취소 |
| 플러그인 버전 고정 | 관리 상세에서 받은 `실습 5` Export 파일 | `workflow.plugin_dependencies` |
| 승인 JS 라이브러리 버전 고정 | 관리 상세에서 받은 `실습 4` Export 파일 | `workflow.script_library_dependencies`의 `lodash@4.17.21` |
| Secret 원문 제외 | Export 파일 | `security.secrets_policy: redacted`, `security.redacted_paths` |
| 파일과 웹 기록 대조 | 워크플로우 관리 상세 | Template ID·Version 일치, 생성자·최근 수정자 |

발표 멘트: “Export는 단순 그래프 복사가 아니라 설계 버전, 변경 메모, 플러그인 의존성, Secret 제거 정책까지 포함한 전달 단위입니다.”

성공 기준: `실습 5` Export JSON에서 `definition_id`, `version`, `plugin_dependencies`, `secrets_policy`를 확인하고, 워크플로우 관리 화면의 Template ID·Version과 일치함을 확인한다. 승인 JS 라이브러리는 `실습 4` Export에서 별도로 확인한다.

## 5. 회의 안건 3 — 명령어 노드 수행 결과를 터미널로 보기

**목표:** 허용된 명령만 실행하고 그 결과를 웹 터미널 형태로 추적할 수 있음을 보여준다.

**로그인:** `admin` → `demo-secadmin`
**사용 데이터:** `실습 7 · 명령 실행 터미널`

1. `admin`으로 **플랫폼 설정 → 명령어 관리**를 연다.
2. 명령 목록에서 아래 세 유형을 차례로 짚는다.
   - `builtin.echo`: 고정 인자 `%s` 뒤에 동적 `message` 하나를 전달하는 가장 단순한 예
   - `demo.request_summary`: `request_id → target_system → action`처럼 여러 동적 인자의 전달 순서를 고정하는 예
   - `demo.path_label`: 고정 인자 `--` 뒤에 동적 `path`를 전달하는 예. `/usr/bin/basename`은 파일을 읽지 않고 경로의 마지막 이름만 출력한다.
3. `demo.request_summary`를 선택해 최종 인자가 `Fixed Args` 뒤에 `Argument Order` 순서대로 붙는다고 설명한다. 예: `/usr/bin/printf '요청번호=%s | 대상=%s | 작업=%s' REQ-2026-001 '개발 포털' '권한 부여'`.
4. `builtin.echo`의 실행 파일, 허용 인수, 제한 시간과 최대 출력 크기를 보여준다.
5. `demo-secadmin`으로 워크플로우 설계에서 `실습 7 · 명령 실행 터미널`을 연다.
6. 명령 노드를 선택해 Arguments JSON의 `"message": "{{formData.message}}"`를 보여준다. 시작 폼에서 받은 값을 명령 인자로 넘기는 바인딩이다.
7. **실행**을 누르고 `출력 메시지`를 `금요일 발표 서버 정상`으로 바꾼 뒤 **제출**한다.
8. 실행 패널의 명령 완료 항목에서 터미널 카드가 나타날 때까지 기다린다.
9. `$ builtin.echo`, stdout의 `금요일 발표 서버 정상`, stderr, 종료 코드 `0`, 소요 시간, 출력 경로를 확인한다.
10. **터미널 출력 복사** 버튼을 보여준다.
11. 실행 패널을 닫아도 캔버스의 완료 상태가 유지되는 것을 보여주고, **실행 표시 지우기**로 명시적으로 초기화한다.

**보조 시연 — 명령 결과로 실제 분기하기(약 1분):**

12. `실습 8 · Node.js 버전 점검 분기`를 연다.
13. `Node.js 버전 확인` 명령 노드가 `builtin.node_version`을 실행하고 결과를 `nodeVersion`에 저장하는 것을 보여준다.
14. 다음 JS 노드가 stdout의 `v22.22.2` 같은 값을 파싱해 `{ raw, major, minimum: 20, supported }`를 반환하는 것을 보여준다.
15. 실행하면 현재 서버는 `20 이상` 경로의 `지원 버전 확인`으로 끝나며, 실행 패널에서 명령 stdout과 JS Console output을 함께 확인한다.

발표 멘트: “명령 결과는 터미널에만 표시되는 로그가 아닙니다. outputPath에 구조화해 저장하고, 후속 JS 노드와 Gateway가 실제 의사결정 데이터로 사용할 수 있습니다.”

주의: Arguments JSON이나 노드 설정을 직접 바꿨다면 먼저 **저장**해야 한다. 실행은 마지막 저장 버전을 사용하며, 미저장 변경이 있으면 화면에서 저장을 먼저 안내한다. 발표 중에는 Arguments JSON을 수정하지 말고 실행 폼의 `출력 메시지`만 바꾼다.

같이 보여주는 기능:

- 허용 목록 기반 명령 실행
- 명령 결과 보존과 웹 터미널 출력
- 시작 폼 입력값을 `{{formData.message}}`로 명령 인자에 전달
- 노드 유형 색상과 실행 상태 색상 분리
- 실행한 경로 강조, 미실행 분기 중립 표시
- 읽기 전용 실행 이력 추적

발표 멘트: “임의 셸 문자열을 실행하지 않습니다. 관리자가 등록한 명령 ID와 검증된 인수만 실행하고 결과는 감사 가능한 형태로 남깁니다.”

질문 대비:

- `Workspace IDs`는 리눅스 디렉터리가 아니라 PXM 내부의 논리적인 업무 공간 범위다. 실제 실행 폴더는 `Working Directory`다.
- 현재 데모에서는 Workspace를 나누지 않으므로 비워 둔다. 현 버전의 명령 Workspace 값은 관리 메타데이터까지 구현되어 있고 선택·실행 강제는 후속 보안 범위다.
- `Fixed Args`는 관리자가 바꾸지 못하게 고정한 인자이고, `Argument Order`는 워크플로우 Arguments JSON 값을 실행 파일에 넘기는 순서다.

## 6. 회의 안건 4 — SSH 플러그인

**목표:** SSH 접속 정보와 서버 신뢰(Host Key)를 자격증명으로 분리하고, 실제 원격 서버에서 명령을 실행해 그 결과를 후속 노드가 쓰는 것까지 보여준다.

**로그인:** `admin` 또는 `demo-secadmin`
**사용 데이터:** 직접 만든 SSH 자격증명, `실습 9 · SSH 원격 명령 실행`

**사전 준비:** 접속 가능한 SSH 서버 한 대와 그 서버의 계정이 필요하다. 사내 테스트 서버를 쓰고, 발표 전에 한 번 접속해 둔다.

### 6-1. SSH 자격증명을 만든다

1. **연동 자격증명 → 신규**를 누르고 Type에서 `SSH`를 선택한다.
2. 관리 Group을 고르고 Host, Port, Username을 입력한다.
3. **서버 키 가져오기**를 누른다. `SHA256:...` Host Key Fingerprint가 자동으로 채워진다.
4. 인증 방식을 고른다. 비밀번호 또는 개인키(Passphrase 선택)를 넣는다.
5. 저장한다. 저장 후 다시 열면 비밀값 칸은 비어 있고 `저장된 값 유지`로 표시된다. 원문은 화면으로 다시 볼 수 없다.

말할 내용: “서버 지문을 미리 고정해 두기 때문에, 같은 주소로 다른 서버가 응답하면 비밀번호를 보내기 전에 접속을 끊습니다. 사람이 `yes`를 눌러줄 수 없는 자동 실행에서 중간자 접속을 막는 방법입니다.”

### 6-2. 실습 9를 준비한다

`실습 9 · SSH 원격 명령 실행`은 **그룹에 SSH 자격증명이 있을 때만** 생성된다. 접속 대상이 환경마다 다르기 때문에 자격증명은 발표자가 만들고, 시드는 그것을 실습 9에 연결해 배포한다.

```bash
# 6-1에서 SSH 자격증명을 만든 뒤 실행한다
pnpm demo:seed
```

- SSH 자격증명이 없으면 `실습 9를 건너뜁니다`라는 경고만 남고 나머지 시연 데이터는 정상 준비된다.
- 그룹에 SSH 자격증명이 여러 개면 **첫 번째 활성 자격증명**이 연결된다. 특정 자격증명을 쓰려면 디자이너에서 노드를 열어 바꾸고 저장·배포한다.

1. **워크플로우 설계**에서 `실습 9 · SSH 원격 명령 실행`을 연다.
2. `원격 서버 상태 확인` 노드를 선택해 **연결된 SSH 자격증명**과 Command를 보여준다.

```text
hostname && uname -sr && ls -1 {{formData.target_path}} | wc -l
```

3. `{{formData.target_path}}`가 시작 폼 입력값을 원격 명령 인자로 넘기는 바인딩임을 설명한다.
4. **연결 테스트**를 눌러 터미널에 stdout, stderr, 종료 코드가 표시되는 것을 확인한다.

### 6-3. 실행해서 결과를 확인한다

1. **실행**을 누르고 `확인할 원격 경로`를 `/tmp`로 두고 제출한다. 대상 서버에 있는 경로면 무엇이든 된다.
2. 실행 패널에서 SSH 노드 출력을 연다.

```json
{
  "exit_code": 0,
  "stdout": "<원격 호스트명>\nLinux 5.14.0-...\n4\n",
  "stderr": "",
  "duration_ms": 153
}
```

3. 다음 JS 노드 `원격 결과 정리`의 출력을 연다. 원격 stdout이 구조화된 값으로 바뀐다.

```json
{
  "hostname": "<원격 호스트명>",
  "kernel": "Linux 5.14.0-...",
  "entryCount": 4,
  "exitCode": 0
}
```

4. `hostname`이 발표 PC가 아니라 **접속한 원격 서버의 이름**이라는 점을 짚는다.

발표 멘트: “원격 명령 결과는 화면에만 찍히는 로그가 아닙니다. outputPath에 저장해 후속 노드와 분기가 실제 판단 데이터로 사용합니다. 5장의 명령 실행과 같은 구조이고 실행 위치만 원격입니다.”

같이 보여주는 기능:

- 비밀값 암호화 저장과 원문 비노출
- 비밀번호·개인키 인증 지원
- Host Key Fingerprint 고정과 서버 키 자동 조회
- SSH 테스트와 실행 결과의 stdout/stderr·종료 코드
- 원격 출력의 후속 노드 전달

성공 기준: `실습 9` 실행이 완료되고 `remoteSummary.hostname`이 원격 서버 이름으로 채워진다.

**SSH 서버를 준비하지 못했다면** 6-1의 자격증명 폼과 **서버 키 가져오기**까지만 보여주고 저장하지 않는다. 실행은 건너뛰고 “원격 실행은 명령 노드와 같은 구조로 동작한다”고 설명한다.

## 7. 회의 안건 5 — JS module import

**목표:** 임의 npm import가 아니라 관리자가 검증·승인한 정확한 버전만 워크플로우에서 사용함을 보여준다.

**로그인:** `admin` → `demo-secadmin` → `demo-requester1`
**사용 데이터:** `lodash@4.17.21`, `실습 4 · 승인된 JS 라이브러리 사용`

1. `admin`으로 **플랫폼 설정 → JS 라이브러리**를 연다.
2. `lodash@4.17.21`의 준비·승인 상태, 번들 크기, 해시, 허용 그룹을 보여준다.
3. 패키지 이름에 `dayjs`를 입력하고 **최신 버전 조회**를 눌러 조회 시점의 정확한 버전이 채워지는 것까지만 보여준다. 준비 버튼은 누르지 않는다.
4. `demo-secadmin`으로 워크플로우 설계에서 `실습 4`를 연다.
5. JS 노드에서 승인된 `lodash@4.17.21` 연결과 `libs['lodash']` 사용 코드를 보여준다.
6. 노드 카드에 라이브러리와 코드 설정이 요약되는 것을 확인한다.
7. `demo-requester1`로 **요청하기**에서 같은 워크플로우를 선택하고 `lodash 숫자 집계` 프리셋을 적용해 제출한다.
8. **내 요청** 완료 결과에서 다음 값을 확인한다.

```json
{
  "count": 5,
  "unique": [12, 7, 30],
  "sortedUnique": [7, 12, 30],
  "total": 68
}
```

발표 멘트: “실행 중 npm에 접속하지 않고, 관리자가 검증한 정확한 버전과 해시의 번들을 배포 버전에 고정해 사용합니다.”

## 8. 회의 안건 6 — 권한·이력을 그룹별로 관리하고 그룹 삭제 시 워크플로우도 처리

**목표:** 그룹이 사용자, 워크플로우, 서비스 계정, API Key, 이력의 권한 경계임을 보여준다.

**로그인:** `admin` → `demo-secadmin`

1. `admin`으로 **사용자 및 권한**을 열고 `데모 · 보안운영팀`을 선택한다.
2. 그룹 멤버, 서비스 계정, API Key, 외부 승인자 매핑이 같은 그룹 기준으로 나뉘는 것을 보여준다.
3. **외부 승인자 매핑**에서 `pxm-demo-hr / partner-manager-01 → demo-approver1` 연결을 보여준다.
4. **삭제 영향 확인**을 누른다. 포함 워크플로우, 활성 API Key, 서비스 계정과 사용 이력을 확인하고 삭제는 취소한다.
5. 사용 기록이 없는 그룹은 영구 삭제되고, 이력이 있는 그룹은 복구 가능한 삭제가 적용됨을 설명한다.
6. 복구 시 워크플로우와 자동 실행은 비활성 상태로 검토하며, 삭제로 비활성화된 API Key는 자동 복구하지 않는다는 안내를 보여준다.
7. `demo-secadmin`으로 전환해 자신이 맡은 그룹만 보이고 **새 그룹**, **삭제 영향 확인** 같은 최고관리자 작업은 보이지 않는 것을 확인한다.
8. **감사 로그**에서 그룹 범위의 workflow, task, API 관련 행위 주체와 시각을 확인한다.

같이 보여주는 기능:

- 그룹별 외부 사용자 매핑
- 동적 다단계 결재의 ALL/ANY와 내부·이메일 다중 채널
- 결재 보류·반려·취소와 전체 이력
- 대리 결재, 원래 승인자, 위임 사유
- 승인 기한 독촉과 상위 알림
- 그룹 범위 감사 로그와 읽기 전용 실행 추적

발표 멘트: “그룹 삭제는 목록 한 줄만 없애는 작업이 아닙니다. 실행 경계에 포함된 워크플로우와 키를 함께 비활성화하고 과거 이력은 보존합니다.”

## 9. 회의 안건 7과 12 — 그룹부터 API Key까지 현장에서 만들고 사용 이력과 차단 확인

**목표:** 준비된 데이터를 조회만 하지 않는다. 발표 중에 **그룹 → 사용자 → 서비스 계정 → API Key를 직접 만들고**, 그 키로 API를 호출하고, 이력을 확인하고, **권한이 다른 키가 막히는 것과 그 실패까지 이력에 남는 것**을 한 흐름으로 보여준다.

회의 안건 7(그룹별 API Key와 사용 주체 추적)과 안건 12(현장 생성)를 이 한 장면에서 함께 끝낸다. 같은 발급·호출·이력 화면을 두 번 보여주지 않기 위해 합쳤다.

**로그인:** `admin` → `demo-live-manager`(발표 중 생성) + API Playground
**사용 데이터:** `실습 4 · 승인된 JS 라이브러리 사용`, `데모 · API 결재 키`
**흐름:** 그룹 생성·삭제 → 그룹 관리자 → 서비스 계정 → 키 발급 → 호출 → 이력 → 차단 → 이력 → 폐기
**권장 시간:** 11분. 시간이 부족하면 9-1을 건너뛰고 9-2부터 시작한다.

### 9-1. 최고관리자가 빈 그룹을 직접 생성하고 삭제한다

**로그인:** `admin`

1. **사용자 및 권한 → 새 그룹**을 누른다.
2. 다음 값을 입력한다.

| 항목 | 값 |
|---|---|
| 그룹 이름 | `발표 · 임시 검증 그룹` |
| 설명 | `발표 중 그룹 생성과 삭제 정책 확인` |

3. 생성된 그룹을 선택해 멤버·서비스 계정·API Key가 모두 0개임을 보여준다.
4. **삭제 영향 확인**을 누른다.
5. 사용 기록이 없으므로 **영구 삭제**가 적용된다는 안내를 확인하고 실제로 삭제한다.

말할 내용: “8장에서 본 `데모 · 보안운영팀`은 이력이 있어 복구 가능한 삭제였습니다. 비어 있고 사용 이력이 없는 그룹만 이렇게 영구 삭제됩니다.”

### 9-2. 실제 로그인 가능한 그룹 관리자를 만든다

1. `데모 · 보안운영팀`을 선택한다.
2. 상단 **사용자 관리 → 새 사용자 추가**를 누른다.
3. 다음 값을 입력한다.

| 항목 | 값 |
|---|---|
| 초기 소속 그룹 | `데모 · 보안운영팀` |
| 사용자 ID | `demo-live-manager` |
| 사용자 이름 | `발표 · 현장 그룹 관리자` |
| 이메일 | `demo-live-manager@pxm.local` |
| 초기 비밀번호 | `PxmDemo!2026` |
| 그룹 역할 | `그룹 관리자` |

4. 생성 후 사용자 상세에서 그룹 소속과 역할을 확인한다.
5. 별도 브라우저 프로필에서 `demo-live-manager / PxmDemo!2026`으로 로그인한다.
6. 새 그룹 생성 메뉴는 없지만 `데모 · 보안운영팀`의 멤버·서비스 계정·API Key는 관리할 수 있음을 확인한다.

### 9-3. 그룹 관리자가 서비스 계정과 최소 권한 키를 발급한다

1. `demo-live-manager`로 **사용자 및 권한 → 서비스 계정 → 서비스 계정 생성**을 누른다.
2. 다음 값을 입력한다.

| 항목 | 값 |
|---|---|
| 서비스 계정 ID | `demo-live-client` |
| 표시 이름 | `발표 · 현장 API 클라이언트` |
| 사용 목적 | `발표 중 직접 발급한 API Key 실행 주체` |

3. **API Key → API Key 발급**을 누르고 1단계 소유자에 다음을 입력한다.

| 항목 | 값 |
|---|---|
| Key 이름 | `발표 · 현장 발급 키` |
| 소유자 유형 | `서비스 계정` |
| 소유자 | `발표 · 현장 API 클라이언트` |

4. 2단계 권한에서 `조회 (읽기 전용)`와 `워크플로우 실행`을 선택한다. 각 항목의 설명 문장을 한 번 읽어 준다. 조회는 **읽기만**이고 실행을 포함하지 않는다.
5. `결재 승인·반려`는 소유자가 서비스 계정이면 **선택 자체가 막혀 있고** 그 이유가 항목 아래에 표시된다. 사람이 하는 결재를 시스템 계정에 줄 수 없다는 점을 한 문장으로 말한다.
6. 접근 범위는 **선택한 워크플로우만**으로 두고 `실습 4 · 승인된 JS 라이브러리 사용`만 고른다.
7. 3단계에서 분당 요청 제한을 `60`으로 입력하고 검토 내용을 읽은 뒤 발급한다.
8. 한 번만 표시되는 원문을 **API Key 복사**로 복사한다. 화면 공유 중에는 메모장이나 메신저에 붙여넣지 않는다.
9. 발급된 카드에서 **소유자는 `demo-live-client`, 발급자 ID는 `demo-live-manager`**로 따로 표시되는 것을 보여준다.

말할 내용: “그룹 관리자가 발급했지만 키 소유자는 그룹 자체가 아니라 서비스 계정입니다. 이 키는 선택한 워크플로우 하나만 조회·실행할 수 있습니다.”

### 9-4. 방금 발급한 키로 실제로 호출한다

1. API Playground `http://localhost:5175`를 연다. 이미 연결되어 있으면 우측 상단 **연결 설정**을 누른다.
2. 다음 값으로 연결한다.

| 항목 | 값 |
|---|---|
| API Base URL | `/api/v1` |
| API Key | 방금 복사한 원문 |
| Business Actor JSON | `{"id":"DEMO-LIVE-001","name":"Live Demo Requester","provider":"presentation"}` |

3. Workflows에 허용한 `실습 4`만 보이는지 확인한다. 다른 실습은 **목록에 나오지도 않는다**.
4. **실행**을 누르고 입력을 다음으로 바꾼 뒤 요청을 보낸다.

```json
{
  "numbers": "10, 20, 10, 5"
}
```

5. **Instances**에서 새로고침해 완료된 새 인스턴스를 연다.
6. Trace와 Result에서 `count: 4`, `unique: [10, 20, 5]`, `total: 45`를 확인한다.
7. **API Console**에서 실제 Endpoint, 요청·응답, Request ID, 복사용 cURL을 보여준다.

### 9-5. 성공 호출의 이력을 확인한다

1. `demo-live-manager` 화면으로 돌아가 **사용자 및 권한 → API Key → 사용 이력**을 열고 **이력 새로고침**을 누른다.
2. API Key 필터에서 `발표 · 현장 발급 키`를 선택한다.
3. 한 행에서 다음을 차례로 짚는다.

| 열 | 확인할 값 |
|---|---|
| 시각 / 키 | 방금 시각, `발표 · 현장 발급 키` |
| 인증된 소유자 | 서비스 계정 `demo-live-client` |
| 호출 경로 / 요청 ID | `POST /api/v1/templates/<실습 4 ID>/execute`, Playground API Console과 같은 Request ID |
| API 요청 결과 | 초록색 `API 요청 성공`과 처리 시간 |
| IP / 외부 전달 정보 | 호출 IP와 Business Actor `DEMO-LIVE-001 / Live Demo Requester / presentation` |

4. Business Actor는 **외부 시스템이 전달한 업무 담당자 표시값이고 인증 주체가 아니다**라고 구분해 준다. 인증 주체는 그 왼쪽의 소유자 열이다.
5. **실행 모니터링**에서 방금 실행한 인스턴스를 찾아 읽기 전용 추적으로 연다. 실행 패널을 닫아도 캔버스 상태가 유지되는 것을 보여준다.

### 9-6. 권한이 다른 키는 막히는 것을 보여준다

결재 담당자의 키로 실행을 시도하는 장면이다. 키 자체는 정상이고 **권한만 다르다**.

1. Playground의 **연결 설정**에서 API Key만 `.env.demo-access.json`의 `approver` 값(`데모 · API 결재 키`)으로 바꿔 다시 연결한다.
2. Workflows 목록이 통째로 바뀐다. 조금 전 `실습 4`는 사라지고 이 키에 허용된 `실습 1`만 보인다.
3. `실습 1` 카드의 **실행**을 누르고 아무 입력으로 요청을 보낸다.
4. 빨간 배너에서 다음을 확인한다.

```text
HTTP 403 · MISSING_SCOPE · workflow:execute scope is required · 요청 ID ... · API Key의 scope를 확인하세요.
```

5. **여기서 멈추지 말고, 막힌 것이 실행뿐임을 바로 이어서 보여준다.** 같은 키로 조회는 정상이다.
   - **API Console**을 연다. 방금 두 호출이 위아래로 나란히 남아 있다.

     | 호출 | 결과 |
     |---|---|
     | `GET /templates` (목록 조회) | `200` |
     | `POST /templates/{id}/execute` (실행) | `403` |

   - 같은 키, 같은 워크플로우인데 **조회는 통과하고 실행만 막혔다**는 것을 두 줄로 보여준다.
   - **Instances**와 **Approval History** 탭도 열어 오류 없이 조회되는 것을 확인한다. `실습 1` 실행 이력이 아직 없으면 목록은 비어 있다. **비어 있는 것은 권한이 없어서가 아니라 데이터가 없는 것**이다. 14장을 먼저 진행했다면 여기에 실행과 결재 건이 보인다.
6. 화면을 가리키며 정리한다. “조금 전 실행은 403으로 막혔는데 조회 요청은 200입니다. 권한이 전부 아니면 전무가 아니라 **동작 단위로 나뉘어 있다**는 뜻입니다.”
7. 두 키의 차이를 이 시점에 비교한다.

| 키 | 소유자 | 권한 | 보이는 워크플로우 | 조회 | 실행 | 결재 |
|---|---|---|---|---|---|---|
| 발표 · 현장 발급 키 | 서비스 계정 `demo-live-client` | 조회 + 실행 | `실습 4`만 | ✅ | ✅ | ❌ (서비스 계정은 부여 불가) |
| 데모 · API 결재 키 | 사용자 `demo-approver1` | 조회 + 결재 | `실습 1`만 | ✅ | ❌ 403 | ✅ |

말할 내용: “같은 그룹의 키인데 보이는 워크플로우도 다르고 할 수 있는 일도 다릅니다. 화면을 감춘 것이 아니라 API가 키의 권한을 동작마다 다시 검증합니다.”

**질문 대비:** 발급 화면의 권한 이름이 헷갈린다는 질문이 나오면 이렇게 답한다. `조회 (읽기 전용)`는 말 그대로 읽기만이고 실행은 포함하지 않는다. 그래서 조회만 가진 키는 목록에 워크플로우가 보이더라도 실행에서 403이 난다. 방금 본 장면이 그 증명이다.

### 9-7. 막힌 호출도 이력에 남는 것을 확인한다

1. PXM의 **사용 이력**으로 돌아가 **이력 새로고침**을 누른다.
2. API Key 필터를 `전체 키`로 둔다.
3. 같은 화면에서 두 행을 위아래로 비교한다.
   - 초록색 `API 요청 성공` — 소유자 `demo-live-client`
   - 빨간색 `API 요청 실패 · 403` — 소유자 `demo-approver1`
4. 실패 행에도 Endpoint, Request ID, 처리 시간, IP가 그대로 남는 것을 보여준다.
5. **감사 로그**에서 조금 전 발급이 `api_key.issued` / 주체 `demo-live-manager`로 남은 것을 확인한다. 사용 이력은 **호출**, 감사 로그는 **관리 행위**라고 구분해 말한다.

말할 내용: “권한 없는 호출은 막고 끝이 아닙니다. 누가 어떤 키로 무엇을 시도했고 몇 번으로 막혔는지가 성공 호출과 같은 화면에 남습니다.”

### 9-8. 마무리로 키를 폐기하고 401을 보여준다

1. PXM의 API Key 목록에서 `발표 · 현장 발급 키`의 **비활성화**를 누른다.
2. Playground에서 같은 원문으로 다시 연결을 시도한다.
3. 연결 화면의 오류에서 `HTTP 401 · UNAUTHORIZED · API key is not active`를 확인한다.
4. 사용 이력을 새로고침해도 **401은 새 행으로 남지 않는다**는 것을 함께 보여준다.
5. 대신 **감사 로그**에 `api_key.disabled`가 주체 `demo-live-manager`로 남아 있는 것을 보여준다.

**주의:** 현재 구현은 **인증에 성공한 호출만** 사용 이력에 적재한다. 폐기된 키나 위조된 키의 401은 이 화면이 아니라 서버 로그에서 확인한다. 질문이 나오면 “403·404는 인증된 키의 권한 위반이라 이력에 남고, 401은 인증 자체가 실패해 주체를 특정할 수 없다”라고 답한다.

### 9-9. 실패 유형별로 무엇이 남는지

| 시도 | 응답 | 사용 이력 |
|---|---|---|
| 결재 키(조회+승인)로 실행 | `403 MISSING_SCOPE` | 남는다. 빨간 실패 행 |
| 실행 키로 허용 목록 밖 워크플로우 실행 | `404 Template not found` — 권한 없는 워크플로우는 **존재 자체를 알려주지 않는다** | 남는다. 빨간 실패 행 |
| 비활성화된 키 또는 위조 키 | `401` | 남지 않는다. 인증 단계에서 차단 |

`404`까지 보여주려면 Playground 목록에 없는 워크플로우를 직접 호출해야 하므로 터미널을 쓴다. 9-8의 비활성화 **전에** 실행한다. `<실습 1 ID>`는 워크플로우 관리 화면의 Template ID 복사 버튼으로 얻는다.

```bash
curl -i -X POST "http://localhost:3011/api/v1/templates/<실습 1 ID>/execute" \
  -H "Authorization: Bearer <발표 · 현장 발급 키 원문>" \
  -H "Content-Type: application/json" \
  -d '{"formData":{"emp_id":"E-1001"}}'
```

시간이 빠듯하면 이 단계는 생략하고, 9-6에서 “키를 바꾸면 보이는 워크플로우 목록 자체가 바뀐다”로 대신한다.

성공 기준:

- API Key 카드의 소유자는 `demo-live-client`, 발급자는 `demo-live-manager`다.
- Playground에는 키마다 허용된 워크플로우 하나씩만 보인다.
- 사용 이력 한 화면에 `성공`과 `실패 · 403`이 서로 다른 소유자로 함께 보인다.
- 실행 모니터링에서 같은 인스턴스의 노드 경로와 결과를 확인한다.
- 비활성화 후 같은 키로 호출하면 `401`이고, 그 401은 이력에 남지 않는다.

리허설 종료 후 `pnpm demo:reset`을 실행하면 `demo-live-manager`, `demo-live-client`, `발표 · 현장 발급 키`와 그 호출 이력이 정리된다.

## 10. 회의 안건 8 — 워크플로우를 누가 생성·변경했는지 확인

**목표:** 설계의 현재 상태뿐 아니라 생성자와 변경자를 추적할 수 있음을 보여준다.

**로그인:** `demo-secadmin`

1. **워크플로우 설계 → 더 보기 → 불러오기**를 연다.
2. 워크플로우 카드의 생성일과 생성자 ID를 가리킨다.
3. **워크플로우 관리**에서 `실습 5`를 선택하고 **작성 이력**의 생성자 / 최근 수정자 / 생성·수정 시각을 보여준다.
4. 버전 목록에서 각 버전 번호·저장 시각과 Version Note를 확인한다.
5. 작성자는 버전 목록이 아니라 **작성 이력**과 **감사 로그**에서 확인한다. 감사 로그에서 같은 워크플로우 ID를 찾아 저장·배포·롤백 주체를 연결해 설명한다.

같이 보여주는 기능: Import 원본 definition/version 추적, 버전 비교, 배포 메타데이터, 실행 당시 워크플로우·그룹·호출자 스냅샷.

## 11. 회의 안건 9 — 역할은 최고관리자와 그룹 관리자로 구분

**목표:** 최고관리자는 그룹을 부여하고, 그룹 관리자는 받은 그룹 안의 리소스와 API 접근을 운영한다는 차이를 보여준다.

1. `admin` 화면에서 플랫폼 설정, 제품 설정, 운영 상태, 전체 감사 로그, 그룹 생성·삭제 메뉴를 보여준다.
2. 사이드바를 접고 아이콘에 마우스를 올려 메뉴 툴팁이 표시되는 것도 함께 보여준다.
3. `demo-secadmin` 화면으로 전환해 자기 그룹의 워크플로우, 사용자 멤버십, 서비스 계정, API Key는 관리할 수 있지만 새 그룹 생성은 할 수 없음을 보여준다.
4. `demo-requester1` 화면으로 전환해 **요청하기 / 내 요청 / 내 결재함** 중심으로 메뉴가 줄어드는 것을 보여준다.
5. 계정 프로필과 세션 정책에서 유휴 시간·최대 세션·강제 종료 정책을 짧게 설명한다.

발표 멘트: “역할별로 화면만 감추는 것이 아니라 API에서도 동일한 그룹·리소스 권한을 검증합니다.”

## 12. 회의 안건 10 — 자주 쓰는 파라미터 세트

**목표:** 반복 입력을 세트로 저장해 두고, 신청자는 **고르기만 해서 실행**한다는 것을 보여준다. 준비된 세트를 쓰는 데서 그치지 않고 **발표 중에 세트를 직접 만들어 그 자리에서 실행**한다.

**로그인:** `demo-secadmin` → `demo-requester1`
**사용 데이터:** `실습 2 · 협력사 접근 권한 신청`
**흐름:** 준비된 세트 확인 → 새 세트 만들기 → 신청자가 불러와 실행 → 실행 경로 확인

### 12-1. 이미 준비된 세트를 확인한다

1. `demo-secadmin`으로 **실행 프리셋** 메뉴를 연다.
2. 세트별 **대상 워크플로우 / 사용 범위 / 입력 키 / 수정일**을 보여준다.
3. 발표 데이터에 준비된 세트는 다음과 같다.

| 세트 이름 | 대상 워크플로우 | API alias | 저장된 입력값 |
|---|---|---|---|
| 저위험 · 자동 처리 | 실습 2 | `demo-auto` | `E-1001 / read / 개발 포털` |
| 고위험 · 내부/외부 승인 | 실습 2 | `demo-review` | `E-2001 / admin / 개발 포털` |
| lodash 숫자 집계 | 실습 4 | `demo-library` | `12, 7, 12, 30, 7` |
| 운영 점검 메시지 출력 | 실습 7 | `demo-command` | `PXM demo command completed` |

4. 같은 워크플로우에 **위험도가 다른 두 세트**가 있는 점을 짚는다. 값을 외우는 대신 상황을 고르는 방식이다.

### 12-2. 발표 중에 새 세트를 직접 만든다

1. 우측 상단 **새 프리셋**을 누른다.
2. 다음 값을 입력한다.

| 항목 | 값 |
|---|---|
| 대상 워크플로우 | `실습 2 · 협력사 접근 권한 신청` |
| 이름 | `발표 · 현장 파라미터 세트` |
| API alias | `demo-live-preset` |
| 설명 | `발표 중 직접 만든 파라미터 세트` |
| 사용 범위 | `소유 그룹` |

3. 입력값 JSON에 다음을 넣는다.

```json
{
  "emp_id": "E-1001",
  "privilege_level": "read",
  "target_system": "발표 데모 포털"
}
```

4. 입력 키가 시작 폼과 맞지 않으면 **저장 버튼이 막히고 오류가 표시된다**는 점을 보여준다. 대상 워크플로우를 고르면 그 워크플로우의 Start 입력 필드를 기준으로 검사한다.
5. **저장**한다. 목록에 새 세트가 나타난다.

말할 내용: “값을 매번 외워서 넣는 대신, 검증된 조합을 세트로 등록합니다. API alias는 외부 시스템이 같은 세트를 부를 때 쓰는 이름입니다.”

**주의:** alias를 `demo-`로 시작하게 두면 다음 `pnpm demo:seed`나 `pnpm demo:reset`에서 자동으로 정리된다. 다른 이름으로 만들면 직접 삭제해야 한다.

### 12-3. 신청자가 불러오기로 실행한다

1. `demo-requester1`로 전환해 **요청하기**에서 `실습 2`를 선택한다.
2. 폼 위쪽 **파라미터 세트** 줄에 세트 버튼들이 보인다. 방금 만든 `발표 · 현장 파라미터 세트`도 함께 있다.
3. 그 버튼을 누른다. `E-1001 / read / 발표 데모 포털`이 폼에 자동으로 채워진다.
4. 신청자는 값을 확인만 하고 **요청 제출**을 누른다.
5. 자동으로 열린 **내 요청**에서 현재 단계와 완료 결과를 확인한다. 대상 시스템이 `발표 데모 포털`로 찍혀 있어 **방금 만든 세트에서 온 값**임을 알 수 있다.

말할 내용: “신청자는 세트를 만들지 않습니다. 관리자가 검증해 둔 조합을 고르고 업무 값만 확인한 뒤 실행합니다.”

**참고:** 신청자도 실행 폼 아래의 **입력값을 새 프리셋으로 저장**으로 세트를 만들 수 있지만, 일반 사용자가 만들면 **본인만 쓰는 개인 세트**가 된다. 그룹 전체가 쓰는 세트는 그룹 관리자가 만든다.

### 12-4. 실행 경로를 확인한다

1. `demo-secadmin`의 **실행 모니터링 → 실시간 추적**으로 이동한다.
2. 직원 조회 출력, 위험도 `AUTO / 0`, 선택된 자동 처리 분기를 순서대로 확인한다.
3. 실행 노드는 상태 색으로, 타지 않은 승인 분기는 중립색으로 남는 것을 보여준다.
4. `고위험 · 내부/외부 승인` 세트로 한 번 더 실행하면 같은 워크플로우가 승인 분기로 가는 것을 비교해 보여줄 수 있다. 이 경우 결재 대기가 남으므로 시간이 없으면 생략한다.

### 12-5. 같은 세트를 API로 부른다 (선택)

세트는 화면 전용이 아니다. 외부 시스템은 **alias 하나로** 같은 입력을 재사용한다.

```bash
curl -X POST "http://localhost:3011/api/v1/templates/<실습 2 ID>/execute" \
  -H "Authorization: Bearer <API Key>" \
  -H "Content-Type: application/json" \
  -d '{"preset":"demo-live-preset"}'
```

- 일부만 바꾸려면 `input`을 함께 보낸다. 세트 값 위에 덮어쓴다.
  `{"preset":"demo-live-preset","input":{"target_system":"운영 포털"}}`
- 화면의 이름이 아니라 **API alias**로 찾는다. 없는 alias면 `404 Input preset not found`다.
- 사용 범위가 `개인 테스트`인 세트는 만든 사람 본인만 쓸 수 있어 서비스 계정 키로는 부를 수 없다.
- 쓰는 API Key의 **허용 워크플로우에 `실습 2`가 들어 있어야 한다**. 9장에서 발급한 키는 `실습 4`만 허용하므로 그대로는 `404`가 난다. 이 단계를 하려면 `실습 2`를 허용한 키를 따로 발급한다.

같이 보여주는 기능:

- 역할별 신청자 포털과 내 요청
- HTTP/DB/JS 노드 출력의 다음 노드 전달
- 완료 결과 표시
- 실시간 실행 그래프와 읽기 전용 과거 실행 추적

성공 기준: 발표 중 만든 세트가 신청자 화면의 파라미터 세트 목록에 나타나고, 그것으로 제출한 실행의 결과에 `발표 데모 포털`이 남는다.

## 13. 회의 안건 11 — 웹 백엔드 JS 보안

이번 발표에서는 **별도 기능 시연에서 제외**한다. 다만 다음 구현 결과는 관련 장면에서 짧게 언급할 수 있다.

- 세션 쿠키, CSRF 검증, 유휴·최대 세션 정책
- 역할·그룹·워크플로우 허용 목록의 API 재검증
- API Key 원문 1회 노출과 저장 시 해시 처리
- Secret 원문 비노출과 Export redaction
- 승인된 JS 라이브러리의 정확 버전·해시·그룹 범위 고정
- 명령 허용 목록, 실행 시간·출력 크기 제한
- SSH Host Key Fingerprint 고정

보안 설계 검토 자체는 별도 회의 안건으로 유지한다.

---

## 14. API 결재 통합 시나리오 — 실행부터 결재 완료까지 API로 처리

**목표:** 외부 시스템이 워크플로우 실행, 결재 상태 조회, 결재 처리, 결과 조회를 공개 API로 끝낼 수 있음을 보여준다.

**준비 키:** `.env.demo-access.json`의 `trigger`, `approver`

1. API Playground에 `trigger` 키로 연결한다.
2. `실습 1 · 기본 접근 권한 결재`만 보이는지 확인하고 다음 입력으로 실행한다.

```json
{
  "emp_id": "E-1001",
  "privilege_level": "read",
  "target_system": "API 포털"
}
```

3. **Instances**에서 상태가 결재 대기인지 확인하고 Instance ID, Trace, 현재 결과를 연다.
4. 연결 설정으로 돌아가 `approver` 키로 다시 연결한다.
5. **Approval History**에서 `OPEN` 상태를 조회하고 상세를 연다.
6. 요청 내용, 워크플로우, 결재자 `demo-approver1`을 확인한다.
7. 의견 `API 결재 시연 승인`을 입력해 승인한다.
8. API Console에서 `Idempotency-Key`, 응답 코드와 Request ID를 확인한다.
9. 다시 `trigger` 키로 연결해 Instances에서 같은 실행이 `COMPLETED`인지 확인한다.
10. PXM의 API Key 사용 이력에서 실행 키와 결재 키의 소유자가 분리되어 기록된 것을 확인한다.

발표 멘트: “외부 시스템용 서비스 계정은 실행만 하고, 사람의 결재 권한은 사용자 소유 키에만 부여했습니다. 하나의 만능 키를 공유하지 않습니다.”

## 15. 필수 결재 시나리오 — 다단계·Webhook과 대리 결재·기한 알림

### 15-1. 다단계 공동 결재와 Webhook

**목표:** 결재 한 건에서 `ALL → ANY`, 보류, 일시중지·재개, 최종 Webhook을 순서대로 보여준다.

**사전 상태:** `pnpm demo:service`가 실행 중이어야 하며 `demo-requester1`, `demo-approver1`, `demo-approver2`, `demo-secadmin`, `admin`을 각각 로그인해 둔다.

1. `demo-requester1`로 **요청하기 → 실습 6 · 다단계 공동 결재**를 연다.
2. `공동 결재 데모 요청` 프리셋을 적용한다. 진행 중인 같은 요청 번호가 있으면 `DEMO-APPROVAL-<현재시각>`으로 바꾼다.
3. 요청을 제출하고 **내 요청**에서 상태가 진행 중인지 확인한다.
4. `demo-approver1`의 **결재함**에서 상세를 열고 **전체 결재라인**을 본다. 1단계는 두 명 `ALL`, 2단계는 두 후보 `ANY`다.
5. **보류**를 고르고 의견을 `변경 범위 추가 확인 필요`로 바꾼 **보류 적용하기**를 누른다. 목록과 처리 이력에 `보류`가 남는다.
6. 같은 결재를 다시 열어 **승인**한다. `demo-approver2`의 미결 결재가 남아 있으므로 아직 2단계가 시작되지 않는다.
7. `demo-secadmin`으로 **실행 모니터링**을 열고 요청 명으로 해당 실행을 찾는다. **일시중지**를 눌러 `PAUSED`를 확인한 뒤 **재개**한다. 결재 대기 정보는 그대로 유지된다.
8. `demo-approver2`가 자신의 1단계 결재를 승인한다. 이제야 2단계가 열린다.
9. `demo-secadmin`의 결재함에서 2단계를 열어 승인한다. `ANY`이므로 외부 이메일 승인자의 나머지 Task는 자동 취소되고 실행은 완료된다.
10. `admin`으로 **결과 Webhook**을 열고 Endpoint `데모 · 결재 결과 수신`으로 필터링한다.
11. 방금 실행의 전송 건을 열어 `전송 완료`, HTTP `200`, 시도 횟수, 실행 ID와 최종 결과를 확인한다.

발표 멘트: “1단계는 두 명 모두가 동의해야 하고, 2단계는 웹 또는 이메일 승인자 중 한 명만 처리하면 됩니다. 사람의 결재가 끝나면 결과는 외부 시스템에 Webhook으로 전달됩니다.”

### 15-2. 대리 결재와 기한 알림

**목표:** 원래 승인자의 부재를 기간·워크플로우 범위의 위임으로 처리하고, 처리 기한이 넘어가면 독촉과 상위 알림으로 연결되는 것을 보여준다.

**사전 상태:** reset이 `demo-approver1 → demo-delegate1`의 실습 3 전용 위임을 활성 상태로 복구한다. 대리 결재자와 그룹 관리자의 이메일이 있어야 메일 발송 이력이 생긴다.

1. `demo-requester1`로 **요청하기 → 실습 3 · 대리 결재와 기한 알림**을 열고 `E-1001 / read / 개발 포털`로 제출한다.
2. `demo-approver1`의 **결재함**에서 이 요청이 보이지 않는지 확인한다. 위임 중인 결재는 원래 승인자가 중복 처리할 수 없다.
3. `demo-delegate1`의 **결재함**에서 같은 요청을 찾는다. 목록의 **대리 결재** 배지를 보여준다.
4. 상세에서 `원래 승인자 demo-approver1`, 위임 종료 시각, 처리 기한을 확인한다.
5. 상단 **대리 결재 설정**을 열어 `demo-approver1 → demo-delegate1`, `실습 3만`, `기존 미결 포함`, 설정자와 설정 사유를 본다. 수정하지 않고 닫는다.
6. **보류를 누르지 않고** 1분을 기다린다. 보류된 Task는 독촉·상위 알림 대상에서 제외되기 때문이다.
7. `admin`으로 **승인 알림**을 열고 **새로고침**을 눌러 `기한 독촉`, 수신자 `demo-delegate1`, 상태 `발송 완료`를 확인한다. Mailpit에서도 같은 독촉 메일을 볼 수 있다.
8. 실행 시작부터 총 2분이 지나면 다시 새로고침해 `상위 알림`, 수신자 `demo-secadmin`을 확인한다.
9. `demo-delegate1`로 돌아가 승인한다.
10. **처리 완료** 이력을 열어 원래 승인자 `demo-approver1`과 실제 처리자 `demo-delegate1`이 따로 남는지 확인한다.

발표 멘트: “위임은 공유 계정이 아니라 원래 승인자와 실제 처리자를 모두 남기는 권한 이전입니다. 대리자가 놓치면 독촉하고, 계속 처리되지 않으면 그룹 관리자에게 올립니다.”

### 15-3. 시간이 남으면 보여줄 운영 가시성

- 대시보드 전체 실행 지표와 갱신 시각
- **운영 상태:** 지금 처리 중인 Job·WAITING·잠금·Outbox의 적체와 실패를 15초마다 보여주고 재시도·잠금 회수·재전송을 수행한다.
- **실행 이상 점검:** 실행·Job·Token·Task·워크플로우 정의 사이의 연결이 깨진 데이터를 필요할 때 정밀 검사하고 안전한 항목만 복구한다.
- 질문에는 “운영 상태는 현재 교통 상황, 실행 이상 점검은 데이터 정합성 건강검진”이라고 구분한다. 두 화면 모두 최고관리자 전용이다.
- 승인자 알림의 수신자·시도 횟수
- 결과 Webhook 상세와 안전한 재전송
- 실행 이상 점검의 고아 Job·Token·Task, 멈춘 실행, 승인 Task 누락, 워크플로우 정의 누락

## 16. 7월 6일 이후 웹 기능 누락 점검

| 기능 | 들어간 시나리오 |
|---|---|
| 다중 워크플로우 탭, 워크플로우 간 복사 | 3 |
| 컨텍스트 메뉴, 빠른 추가, 자동 정렬, 넓게 보기 | 3 |
| 노드 요약, 분기 라벨, 미니맵, 실행 상태 유지 | 3, 5, 12 |
| Export 버전·의존성·Secret redaction | 4 |
| 저장·배포·중지·재활성화·버전 비교·롤백 | 4 |
| 명령 허용 목록과 웹 터미널 | 5 |
| SSH 자격증명·호스트 키·원격 명령 실행 | 6 |
| 승인된 JS 라이브러리와 정확 버전 조회 | 7 |
| 그룹 RBAC, 사용자·멤버십, 외부 사용자 매핑 | 8, 9 |
| 그룹 삭제 영향·복구 검토 | 8, 9 |
| 그룹 API Key, 발급자·소유자·사용 이력 | 9, 14 |
| API Key 권한 위반 차단(403/404)과 실패 이력 | 9 |
| 워크플로우 생성자·편집자·가져온 원본 | 10 |
| 역할별 사이드바·접힌 메뉴 툴팁·보안 세션 | 11 |
| 입력 프리셋 생성·적용, 신청자 포털, 내 요청 결과 | 12 |
| 공개 API v1, 실행·조회·결재·결과 | 9, 14 |
| 다단계 ALL/ANY, 다중 채널, 보류·반려·취소 | 8, 15 |
| 승인 독촉·상위 알림·대리 결재·재배정 | 8, 15 |
| 알림·Webhook·운영 상태·이상 점검·감사 | 8, 15 |
| HTTP 응답 저장과 실행 데이터 참조 | 12, 15 |

웹에서 보여줄 수 있는 7월 6일 이후 주요 기능은 모두 위 시나리오에 연결했다. 웹 백엔드 JS 보안 설계 검토만 별도 안건으로 남겼다.

## 17. 발표 직전 검증과 복구

전체 격리 환경 검증:

```bash
pnpm demo:verify
```

현재 실행 중인 서버와 데이터 검증:

```bash
pnpm demo:check
```

리허설 뒤 복구:

```bash
pnpm demo:reset
```

문제가 생기면 다음 순서로 확인한다.

1. `pnpm demo:service`와 API Playground가 실행 중인지 확인한다.
2. 진행 중인 데모 실행을 완료하거나 종료한다.
3. `pnpm demo:reset`을 다시 실행한다.
4. 계정 비밀번호와 준비 API Key는 `.env.demo-access.json`에서 다시 확인한다.
5. Mailpit에서는 제목과 수신 시각이 가장 최근인 메일을 사용한다.
6. 발표 중 생성한 API Key 원문을 잃었다면 기존 값을 찾으려 하지 말고 Rotation 또는 새 발급을 사용한다.
